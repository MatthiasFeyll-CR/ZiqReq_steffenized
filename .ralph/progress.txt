# Ralph Progress Log — M10 (review)
Started: 2026-03-11T05:56:48+0100

---

## Implementation Log

<!-- Ralph appends per-story entries below as it works. -->
<!-- Each entry should include: story ID, what was done, files changed, -->
<!-- decisions made, and any deviations from the spec. -->

---

## Codebase Patterns

<!-- Ralph records reusable patterns discovered during implementation. -->
<!-- These are extracted by the PRD Writer for future milestones. -->

---

## Deviations & Decisions

<!-- Ralph records any spec deviations or implementation decisions -->
<!-- that the Spec Reconciler should review. -->

## US-001: Submit Idea for Review
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/review/models.py (new — mirror models for BrdVersion, ReviewAssignment, ReviewTimelineEntry), services/gateway/apps/review/views.py (submit_idea endpoint), services/gateway/apps/review/serializers.py (SubmitIdeaSerializer), services/gateway/apps/review/migrations/0001_create_review_tables.py (RunSQL migration), services/gateway/apps/review/tests/test_submit.py (13 tests), services/gateway/apps/ideas/urls.py (added submit route)
- Tests: 13 passed, 0 failed — covers T-1.5.01, T-1.5.05, T-4.7.01, T-4.10.01, T-4.10.02, API-SUBMIT.01-04 plus additional edge cases
- Learnings for future iterations:
  - The review app had placeholder views.py/urls.py/serializers.py — no models.py existed in gateway, only in core service
  - Submit endpoint is routed via ideas URLs (path("<str:idea_id>/submit")) since it's under /api/ideas/:id/submit
  - Mirror models use managed=False with RunSQL CREATE TABLE IF NOT EXISTS in gateway migrations (same pattern as brd_drafts)
  - PdfClient.generate_pdf is a stub that returns {"pdf_data": b"", "filename": "placeholder.pdf"} — mock it in tests with _create_pdf_client
  - ReviewTimelineEntry has immutability enforcement in save() — raises ValidationError on update
  - The full test suite (pytest on all apps) has pre-existing test ordering issues with websocket tests (pass individually, error in batch) — not caused by this change
  - docker compose build gate check requires docker-compose.yml which doesn't exist (only docker-compose.test.yml) — pre-existing
  - User model has `roles` ArrayField — use roles=["user", "reviewer"] for reviewer users in tests
---

## US-002: Self-Assignment & Unassignment API
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/review/views.py (added assign_review, unassign_review, _require_reviewer), services/gateway/apps/review/urls.py (added assign/unassign routes), services/gateway/apps/review/tests/test_review_assignments.py (new — 13 tests)
- Tests: 13 passed, 0 failed — covers T-10.3.01, T-10.3.02, T-10.4.01, API-REVIEW.03, API-REVIEW.04, API-REVIEW.05 plus co-owner conflict, reassign after unassign, invalid states, not-found cases
- Learnings for future iterations:
  - Review app URLs are mounted at /api/reviews/ in gateway/urls.py — use <str:idea_id>/assign and <str:idea_id>/unassign
  - _require_reviewer helper checks user.roles for "reviewer" — reuse for all reviewer-only endpoints
  - Duplicate assignment check: filter by unassigned_at__isnull=True (active assignments only)
  - Reassign after unassign creates a new ReviewAssignment row (old one keeps unassigned_at timestamp)
  - django.utils.timezone.now() used for setting unassigned_at timestamp
  - Test infrastructure requires rebuild (docker compose build python-tests) when adding new test files
  - Node tests have 1 pre-existing error in information-gaps-toggle.test.tsx — not caused by backend changes
---

## US-003: Review Actions API — Accept, Reject, Drop, Undo
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/review/views.py (added accept_review, reject_review, drop_review, undo_review, _handle_review_action helper), services/gateway/apps/review/serializers.py (added ReviewActionCommentSerializer), services/gateway/apps/ideas/urls.py (added review/accept, review/reject, review/drop, review/undo routes), services/gateway/apps/review/tests/test_review_actions.py (new — 19 tests)
- Tests: 19 passed, 0 failed — covers T-1.5.02, T-1.5.03, T-1.5.04, T-1.5.06, T-1.5.07, T-1.5.08, T-1.5.09, API-REVIEW.06, API-REVIEW.07, API-REVIEW.08, API-REVIEW.09 plus blank comment, undo rejected, undo invalid states, non-reviewer forbidden, timeline state recording
- Learnings for future iterations:
  - Review actions routed under /api/ideas/:id/review/ — added in ideas/urls.py (same pattern as submit)
  - Used _handle_review_action shared helper with state machine maps to avoid code duplication across 4 endpoints
  - State machine: accept/reject/drop only from in_review; undo from accepted/dropped/rejected
  - Comment required for reject, drop, undo — validated via ReviewActionCommentSerializer
  - Accept does not require a comment — empty body is fine
  - Multiple reviewers: no consensus, latest action wins (last state change is the current state)
  - Timeline entries created atomically with state change using transaction.atomic()
  - _require_reviewer helper from US-002 reused for all 4 endpoints
---

## US-004: Review Timeline API — GET Entries & POST Comments
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/review/views.py (added review_timeline, _get_timeline, _post_timeline_comment, _serialize_timeline_entry), services/gateway/apps/review/serializers.py (added TimelineCommentSerializer), services/gateway/apps/ideas/urls.py (added review/timeline route), services/gateway/apps/review/tests/test_review_timeline.py (new — 15 tests)
- Tests: 15 passed, 0 failed — covers API-TIMELINE.01-05 plus ordering, empty timeline, null author, parent wrong idea, unauthenticated
- Learnings for future iterations:
  - Timeline endpoint uses combined GET/POST view with method dispatch (review_timeline function)
  - Author info is batch-loaded to avoid N+1 queries — collect author_ids from entries, bulk fetch User objects
  - _serialize_timeline_entry helper converts model to dict with author object {id, display_name} or null
  - Parent entry validation checks both existence AND same idea_id (prevents cross-idea replies)
  - POST returns 201 (not 200) for created comment entries
  - TimelineCommentSerializer validates content (required, non-blank) and optional parent_entry_id (UUID)
  - docker compose build python-tests needed when adding new test files (container copies files at build time)
---

## US-005: Review Page List API
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/review/views.py (added list_reviews endpoint), services/gateway/apps/review/urls.py (added root "" route for list_reviews), services/gateway/apps/review/tests/test_review_list.py (new — 11 tests)
- Tests: 11 passed, 0 failed — covers T-10.1.01, T-10.2.01, API-REVIEW.01, API-REVIEW.02 plus edge cases (other reviewer assignment, unassigned_at handling, metadata fields, open ideas exclusion, unauthenticated, empty list, reviewers list)
- Learnings for future iterations:
  - list_reviews is mounted at the root of /api/reviews/ (path("")) — no idea_id param needed
  - Categories: in_review ideas split into assigned_to_me (current user has active assignment) vs unassigned (no active assignments at all); ideas assigned to other reviewers only don't appear in either
  - Final state ideas (accepted/rejected/dropped) go to their respective categories regardless of assignments
  - Batch-load pattern: collect all owner_ids + reviewer_ids, single User query, build users_map for O(1) lookups
  - submitted_at uses idea.updated_at as proxy (set when state changes to in_review)
  - Open ideas are excluded from all categories (state not in filter)
---

## US-006: Review Page Layout — Frontend
- Status: PASS
- Attempts: 1
- Changes: frontend/src/api/review.ts (new — API client with fetchReviews), frontend/src/components/review/ReviewCard.tsx (new — card component matching IdeaCard pattern), frontend/src/pages/review-page.tsx (replaced placeholder with full page), frontend/src/app/router.tsx (imported ReviewPage component), frontend/src/__tests__/review-page.test.tsx (new — 9 tests)
- Tests: 9 passed, 0 failed — covers T-10.2.02 (categories render with counts), T-10.2.03 (collapsible categories), UI-REVIEW.01 (page navigation with Navbar), UI-REVIEW.02 (empty states)
- Learnings for future iterations:
  - ReviewCard follows same pattern as IdeaCard: button wrapper, state dot, Badge, click navigates to /idea/:id
  - Collapsible categories use simple useState toggle — no shadcn Collapsible needed
  - Default open state: Assigned to me and Unassigned expanded, Accepted/Rejected/Dropped collapsed
  - vi.mock factory hoisting issue: use vi.hoisted() to create mock functions that vi.mock factory references
  - "Reviews" text appears in both navbar link and h1 — use getByRole("heading") to disambiguate in tests
  - Navbar already has role-gated Reviews link (hasRole("reviewer")) — no changes needed
  - Router had placeholder <div>Review Page</div> — replaced with actual ReviewPage import
  - docker compose build node-tests required when adding new test files (container copies at build time)
  - fetchReviews API client follows same pattern as fetchIdeas: credentials include, error handling with status attachment
---

## US-007: Self-Assignment UI
- Status: PASS
- Attempts: 1
- Changes: frontend/src/api/review.ts (added assignReview, unassignReview functions + owner_id/co_owner_id to ReviewIdea type), frontend/src/components/review/ReviewCard.tsx (added Assign/Unassign buttons with loading, error toast, conflict of interest tooltip), services/gateway/apps/review/views.py (added owner_id, co_owner_id to list_reviews API response), frontend/src/__tests__/self-assignment.test.tsx (new — 11 tests), frontend/src/__tests__/review-page.test.tsx (updated test data with owner_id/co_owner_id fields)
- Tests: 11 passed, 0 failed — covers UI-ASSIGN.01 (assign button, API call, no navigate on click, loading state), UI-ASSIGN.02 (unassign button, API call, error toast), UI-ASSIGN.03 (conflict of interest disabled for owner/co-owner, no action on final state categories, error toast with Retry)
- Learnings for future iterations:
  - ReviewCard outer element changed from <button> to <div role="button"> to avoid nested <button> DOM nesting error
  - Conflict of interest check: compare current user.id against idea.owner_id and idea.co_owner_id on frontend side
  - Backend list_reviews API was extended to include owner_id and co_owner_id in response items
  - TooltipProvider wraps individual tooltip usage (no app-level provider) — same pattern as ContextWindowIndicator
  - vi.hoisted() must include ALL mocks referenced in vi.mock factories (toast mock was initially not hoisted, causing ReferenceError)
  - stopPropagation on action button click prevents outer div navigation when clicking Assign/Unassign
  - useMutation with invalidateQueries on ["reviews"] key refreshes the categorized list after assign/unassign
---

## US-008: Review Section Below Fold
- Status: PASS
- Attempts: 1
- Changes: frontend/src/components/review/ReviewSection.tsx (new — section with header + timeline), frontend/src/components/review/ReviewTimeline.tsx (new — chronological timeline entry rendering), frontend/src/pages/IdeaWorkspace/index.tsx (added ReviewSection below fold with auto-scroll), frontend/src/api/review.ts (added fetchTimeline, fetchIdeaReviewers, TimelineEntry, ReviewerInfo types), services/gateway/apps/review/views.py (added get_idea_reviewers endpoint), services/gateway/apps/ideas/urls.py (added review/reviewers route), frontend/src/__tests__/review-section.test.tsx (new — 9 tests), frontend/src/__tests__/idea-workspace.test.tsx (added ReviewSection mock), frontend/src/__tests__/section-visibility.test.tsx (added ReviewSection mock)
- Tests: 9 new tests passed, 337 total node tests passed, 566 python tests passed
- Learnings for future iterations:
  - ReviewSection uses TanStack Query hooks — any test that renders IdeaWorkspace must either mock ReviewSection or provide QueryClientProvider
  - Added vi.mock("@/components/review/ReviewSection") to existing test files (idea-workspace.test.tsx, section-visibility.test.tsx) to prevent useQuery errors
  - "Below fold" pattern: outer container uses overflow-y-auto, workspace layout gets minHeight: calc(100vh - 64px) when review section is present
  - has_been_submitted heuristic: idea.state !== "open" (same as useSectionVisibility)
  - Auto-scroll uses useRef + scrollIntoView({behavior: "smooth"}) with prevStateRef to track state changes
  - Docker containers need rebuild (docker compose up --build) when frontend files change — local tests pass but container may have stale code
  - getByText fails with "multiple elements" when both workspace header mock and ReviewSection render same title — use getByTestId + toHaveTextContent instead
  - Backend get_idea_reviewers endpoint follows same patterns: _require_auth, _get_idea_or_error, batch-load users
---

## US-009: Review Timeline UI
- Status: PASS
- Attempts: 1
- Changes: frontend/src/components/review/ReviewTimeline.tsx (added reply buttons, CommentInput integration, WebSocket invalidation, useMutation for posting), frontend/src/components/review/CommentInput.tsx (new — textarea + send button with cancel/reply support), frontend/src/api/review.ts (added postComment function), frontend/src/components/review/ReviewSection.tsx (pass ideaId to ReviewTimeline), frontend/src/__tests__/review-timeline.test.tsx (new — 14 tests)
- Tests: 337 node tests passed, 14 new timeline-specific tests covering UI-TIMELINE.01-04
- Learnings for future iterations:
  - ReviewTimeline already had 3 entry types (state_change, comment, resubmission) from US-008 — extended with interactive features
  - CommentInput is a reusable component with onSubmit/onCancel/autoFocus/placeholder props
  - Reply workflow: click Reply button → inline CommentInput appears below entry → submit posts with parent_entry_id → invalidates timeline query
  - WebSocket invalidation listens for 'ws:timeline_update' CustomEvent (same pattern as 'ws:brd_ready' in ReviewTab)
  - useMutation with queryClient.invalidateQueries(["timeline", ideaId]) refreshes timeline after comment post
  - Resubmission entry enhanced with download buttons (v-prefix + UUID slice for labels)
  - screen.getAllByTestId()[1] needs non-null assertion (!) for TypeScript strict mode
  - Pre-existing unhandled error in information-gaps-toggle.test.tsx (URL.createObjectURL) does not affect test results
---

## US-010: Submit Flow UI — SubmitArea in Review Tab
- Status: PASS
- Attempts: 1
- Changes: frontend/src/components/review/SubmitArea.tsx (new — message textarea, reviewer multi-select, submit button), frontend/src/components/workspace/ReviewTab.tsx (added SubmitArea integration + ideaState prop), frontend/src/components/workspace/WorkspaceLayout.tsx (added ideaState prop passthrough), frontend/src/pages/IdeaWorkspace/index.tsx (pass idea.state to WorkspaceLayout), frontend/src/api/ideas.ts (added submitIdea function + SubmitIdeaResponse type), frontend/src/api/review.ts (added fetchReviewerUsers function + ReviewerUser type), services/gateway/apps/review/views.py (added list_reviewer_users endpoint), services/gateway/apps/review/urls.py (added reviewers route), frontend/src/__tests__/submit-area.test.tsx (new — 10 tests)
- Tests: 10 new tests passed, 361 total node tests passed, ruff + tsc clean
- Learnings for future iterations:
  - SubmitArea visibility controlled by ideaState prop — renders only for 'open' and 'rejected' states
  - ideaState prop threaded through IdeaWorkspace → WorkspaceLayout → ReviewTab → SubmitArea
  - Reviewer multi-select uses checkbox list fetched from GET /api/reviews/reviewers (new backend endpoint)
  - Backend list_reviewer_users filters by roles__contains=["reviewer"] using PostgreSQL array containment
  - vi.hoisted() pattern used for all mocked functions referenced in vi.mock factories (submitIdea, fetchReviewerUsers, toast)
  - SubmitArea calls invalidateQueries for ["brd", ideaId], ["idea", ideaId], ["timeline", ideaId] on success
  - Pre-existing URL.createObjectURL error in information-gaps-toggle.test.tsx is unrelated
---
