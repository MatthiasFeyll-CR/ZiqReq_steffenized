# Ralph Progress Log — M14 (merge-advanced)
Started: 2026-03-11T13:38:26+0100

---

## Implementation Log

<!-- Ralph appends per-story entries below as it works. -->
<!-- Each entry should include: story ID, what was done, files changed, -->
<!-- decisions made, and any deviations from the spec. -->

---

## Codebase Patterns

<!-- Ralph records reusable patterns discovered during implementation. -->
<!-- These are extracted by the PRD Writer for future milestones. -->

---

## Deviations & Decisions

<!-- Ralph records any spec deviations or implementation decisions -->
<!-- that the Spec Reconciler should review. -->

## US-001: Append Flow Backend — 3-Way Consent
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/ideas/views.py, services/gateway/apps/ideas/serializers.py, services/gateway/apps/ideas/tests/test_append_flow.py (new)
- Tests: 775 Python tests passed, 14 new append flow tests (all pass)
- Learnings for future iterations:
  - Auto-detect merge_type from target idea state: in_review → append, else → merge
  - Append requires reviewer_consent='pending' (3-way), merge uses reviewer_consent='not_required' (2-way)
  - Consent endpoint allows both target_owner AND assigned reviewer for append type; only target_owner for merge type
  - MergeRequestSerializer now includes reviewer_consent field
  - Events: append uses append_request_created/append_accepted/append_declined; merge uses merge_request_created/merge_request_accepted/merge_request_declined
  - ReviewAssignment model is managed=False (mirror of Core table). Creating test ReviewAssignment records works fine in test DB.
  - Transient DB errors with unique constraint uq_active_merge_request across test classes — resolved by rebuilding test container or re-running
  - In tests, avoid creating multiple pending MergeRequests for same (requesting_idea_id, target_idea_id) pair — use different idea pairs
---

## US-002: Append Execution — Close & Collaborator Add
- Status: PASS
- Attempts: 1
- Changes: services/core/apps/similarity/append_service.py (new), services/core/events/consumers.py, services/gateway/apps/similarity/append_service.py (new mirror), services/gateway/events/append_consumer.py (new mirror), services/gateway/apps/ideas/tests/test_append_execution.py (new)
- Tests: 787 Python tests passed, 12 new append execution tests (all pass), 403 Node tests passed
- Learnings for future iterations:
  - Gateway mirrors core services for test discoverability (PYTHONPATH has gateway first, so `apps.similarity` resolves to gateway's version)
  - When creating new service modules in core, also create gateway mirror (same as merge_service.py pattern)
  - Consumer pattern: separate files per event domain (merge_consumer.py, append_consumer.py) in gateway/events/
  - execute_append uses get_or_create for idempotent collaborator addition
  - Append does NOT create new idea, does NOT change state — only flags closed_by_append_id and adds collaborator
  - _publish_append_complete called outside transaction (same pattern as merge_service)
  - Pre-existing flaky test: information-gaps-toggle.test.tsx has error after all tests pass (PDFPreview import issue)
---

## US-003: Manual Merge — UUID/URL Entry
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/ideas/views.py, services/gateway/apps/ideas/serializers.py, services/gateway/apps/similarity/models.py, services/core/apps/similarity/models.py, services/core/apps/similarity/migrations/0003_add_manual_request_flag.py (new), services/gateway/apps/ideas/tests/test_manual_merge.py (new), .ralph/test-manifest.json
- Tests: 800 Python tests passed (13 new manual merge tests), 403 Node tests passed, ruff clean, tsc clean
- Learnings for future iterations:
  - MergeRequestCreateSerializer now accepts target_idea_url OR target_idea_id (both optional, at least one required)
  - URL parsing uses regex `/idea/([0-9a-f-]{36})/` to extract UUID from URL
  - manual_request=True flag set when target_idea_url is provided; False for direct UUID
  - No access check on target idea for merge requests — the original code didn't have one and the similarity system handles discovery
  - Error codes changed from generic to specific: TARGET_NOT_FOUND, CANNOT_MERGE_SELF, INVALID_UUID, INVALID_STATE
  - Target must be non-deleted (deleted_at__isnull=True) and in valid state (open, in_review, rejected)
  - Declined pairs bypass: no check for prior declined MergeRequests, only pending ones are blocked
  - Pre-existing flaky test: idea-workspace.test.tsx T-1.7.01 fails intermittently (unrelated to changes)
---
