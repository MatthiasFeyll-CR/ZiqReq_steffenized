# Ralph Progress Log — M1 (foundation)
Started: 2026-03-09T17:27:54+0100

---

## Implementation Log

<!-- Ralph appends per-story entries below as it works. -->
<!-- Each entry should include: story ID, what was done, files changed, -->
<!-- decisions made, and any deviations from the spec. -->

---

## Codebase Patterns

<!-- Ralph records reusable patterns discovered during implementation. -->
<!-- These are extracted by the PRD Writer for future milestones. -->

---

## Deviations & Decisions

<!-- Ralph records any spec deviations or implementation decisions -->
<!-- that the Spec Reconciler should review. -->

## US-001: Database Schema and pgvector Setup
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/authentication/models.py, services/gateway/apps/notifications/models.py, services/gateway/apps/monitoring/models.py, services/core/apps/ideas/models.py, services/core/apps/chat/models.py, services/core/apps/board/models.py, services/core/apps/brd/models.py, services/core/apps/review/models.py, services/core/apps/collaboration/models.py, services/core/apps/similarity/models.py, services/core/apps/admin_config/models.py, services/ai/apps/context/models.py, services/ai/apps/embedding/models.py, 12 migration files (0001_initial.py per app), 11 migration __init__.py files, frontend/tsconfig.json
- Tests: No automated tests for this story (schema-only). Gate checks: ruff PASS, docker compose build PASS (gateway/core/ai), tsc --noEmit PASS.
- Learnings for future iterations:
  - User.roles uses ArrayField (not JSONField) - requires django.contrib.postgres
  - IdeaKeywords.keywords uses ArrayField (not JSONField)
  - pgvector vector columns require `pgvector` Python package and `CREATE EXTENSION vector` in migration
  - HNSW indexes use pgvector.django.HnswIndex with opclasses=["vector_cosine_ops"]
  - Partial unique indexes (ReviewAssignment, MergeRequest) done via RunSQL in migrations
  - CHECK constraints done via RunSQL (Django choices don't enforce at DB level)
  - Cross-service FK references stored as UUIDField (no Django ForeignKey) since services use separate Django projects
  - ruff --fix can auto-fix F401 (unused imports) in scaffold files
  - Frontend tsconfig needed `"types": ["vite/client"]` for import.meta.env
  - SQL strings in migrations must use string concatenation to stay under 120 char line limit
  - The embedding migration depends on context migration (for the vector extension order)
---

## US-002: Seed Data
- Status: PASS
- Attempts: 1
- Changes: services/core/apps/admin_config/migrations/0002_seed_parameters.py, services/ai/apps/context/migrations/0002_seed_buckets.py, services/gateway/apps/authentication/migrations/0002_seed_dev_users.py
- Tests: No automated tests for this story (seed data only). Gate checks: ruff PASS, docker compose build PASS (gateway/core/ai), tsc --noEmit PASS.
- Learnings for future iterations:
  - Seed migrations use RunPython with apps.get_model() for forward-compatibility
  - get_or_create used for idempotent seed data (safe to re-run)
  - Dev users conditional on os.environ DEBUG check (not Django settings, since migrations don't have full settings context)
  - AI parameters with deployment-specific defaults use empty string as placeholder
  - 21 admin parameters total: 10 application, 3 infrastructure, 8 AI
  - Singleton buckets use .exists() guard before creating
  - Dev user UUIDs use deterministic format (00000000-0000-0000-0000-00000000000N) for test stability
  - ruff I001 (import sorting) auto-fixable with --fix in migration files
  - PDF docker build fails (pre-existing issue) — build gateway/core/ai individually
---

## US-003: Gateway Django Models
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/notifications/models.py, services/gateway/apps/monitoring/models.py
- Tests: No automated tests for this story (model-only). Gate checks: ruff PASS, tsc --noEmit PASS, docker compose build PASS (gateway/core/ai).
- Learnings for future iterations:
  - Gateway models (User, Notification, MonitoringAlertConfig) were largely created in US-001 schema story
  - US-003 added __str__ methods to Notification and MonitoringAlertConfig for Django admin/debugging usability
  - User model already had __str__ from US-001
  - docker-compose.yml is at infra/docker/docker-compose.yml, not project root
---

## US-004: Core Django Models
- Status: PASS
- Attempts: 1
- Changes: services/core/apps/ideas/models.py, services/core/apps/chat/models.py, services/core/apps/board/models.py, services/core/apps/brd/models.py, services/core/apps/review/models.py, services/core/apps/collaboration/models.py, services/core/apps/similarity/models.py, services/core/apps/admin_config/models.py, services/core/apps/review/migrations/0002_add_constraint_state.py, services/core/apps/similarity/migrations/0002_add_constraint_state.py
- Tests: No automated tests for this story (model-only). Gate checks: ruff PASS, tsc --noEmit PASS, docker compose build PASS (gateway/core/ai).
- Learnings for future iterations:
  - Core models were largely created in US-001 schema story; US-004 added __str__, immutable save(), and Meta.constraints
  - Immutable models (ChatMessage, BrdVersion, ReviewTimelineEntry) override save() using `self._state.adding` check — raises ValidationError if not a new record
  - Partial unique constraints added via Meta.constraints with UniqueConstraint(condition=...) for ReviewAssignment and MergeRequest
  - Since RunSQL indexes already existed from US-001, used SeparateDatabaseAndState in new migrations to sync Django state without touching DB
  - `type: ignore[override]` comment needed on save() overrides to pass mypy (signature differs from Model.save)
---

## US-005: Authentication Middleware and Dev Bypass
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/authentication/views.py, services/gateway/apps/authentication/middleware.py, services/gateway/apps/authentication/urls.py, services/gateway/apps/authentication/azure_ad.py, services/gateway/apps/authentication/tests/test_dev_bypass.py, services/gateway/apps/authentication/tests/test_azure_ad.py, services/gateway/gateway/settings/base.py, services/gateway/gateway/settings/test.py, requirements.txt, services/gateway/requirements.txt, pyproject.toml, .ralph/test-manifest.json
- Tests: 14 passed (13 auth tests + 1 smoke). Gate checks: ruff PASS, tsc --noEmit PASS, docker compose build PASS (gateway/core/ai).
- Learnings for future iterations:
  - Added PyJWT[crypto] for Azure AD JWT validation (RS256 signature verification)
  - Azure AD token validation uses JWKS endpoint with 24h cache
  - Dev bypass is double-gated: both DEBUG=True AND AUTH_BYPASS=True required
  - Session support needed: added django.contrib.sessions to INSTALLED_APPS and SessionMiddleware
  - Auth middleware added to MIDDLEWARE list in settings (apps.authentication.middleware.AuthenticationMiddleware)
  - Test settings (test.py) updated to parse DATABASE_URL for PostgreSQL in docker test environment
  - ArrayField requires PostgreSQL — SQLite fallback in test.py won't work for User model
  - DJANGO_SETTINGS_MODULE added to root pyproject.toml [tool.pytest.ini_options] for pytest discovery
  - Auth endpoints (validate, dev-users, dev-login, dev-switch) are exempt from middleware auth checks
  - Tests use unittest.mock.patch to mock _get_signing_key for Azure AD JWT validation
  - RSA key generation in tests uses cryptography library (comes with pyjwt[crypto])
  - override_settings decorator used in tests to toggle DEBUG/AUTH_BYPASS per test
---

## US-006: gRPC Service Definitions and Stubs
- Status: PASS
- Attempts: 1
- Changes: proto/common.proto, proto/core.proto, proto/ai.proto, proto/gateway.proto, proto/pdf.proto, proto/Makefile, proto/__init__.py, proto/*_pb2.py, proto/*_pb2_grpc.py, proto/*_pb2.pyi, scripts/generate-proto.sh, services/core/grpc_server/server.py, services/core/grpc_server/servicers/core_servicer.py, services/gateway/grpc_server/server.py, services/gateway/grpc_server/servicers/gateway_servicer.py, services/ai/grpc_server/server.py, services/ai/grpc_server/servicers/processing_servicer.py, services/ai/grpc_server/servicers/context_servicer.py, services/pdf/grpc_server/server.py, services/pdf/grpc_server/servicers/pdf_servicer.py, services/gateway/grpc_clients/core_client.py, services/gateway/grpc_clients/ai_client.py, services/gateway/grpc_clients/pdf_client.py, services/gateway/grpc_clients/enrichment.py, services/ai/grpc_clients/core_client.py, services/notification/grpc_clients/gateway_client.py, services/notification/grpc_clients/core_client.py
- Tests: No automated tests for this story (stubs only). Gate checks: ruff PASS, tsc --noEmit PASS, docker compose build PASS (gateway/core/ai/notification).
- Learnings for future iterations:
  - Proto files live in proto/ at project root; generated code also goes there
  - generate-proto.sh compiles all .proto files with --python_out, --grpc_python_out, --pyi_out
  - Docker build context for services does NOT include proto/ — servicers/clients must not import generated proto code at module level
  - For M1, servicers return plain dicts as placeholders; actual proto message objects will be used when wiring in later milestones
  - AI service has two servicer files: processing_servicer.py (chat, BRD, reindex, metrics) and context_servicer.py (bucket management)
  - Port assignments: core=50051, ai=50052, pdf=50053, gateway=50054
  - grpcio and grpcio-tools already in all service requirements.txt files
  - ruff auto-fix (--fix) handles I001 import sorting in server.py files
  - PDF Dockerfile build fails (pre-existing libgdk-pixbuf2.0-0 issue) — not a US-006 concern
  - google/protobuf/empty.proto import only needed when using google.protobuf.Empty (ai.proto uses it for bucket getters)
---
## US-007: UI Primitive Components
- Status: PASS
- Attempts: 1
- Changes: frontend/package.json, frontend/package-lock.json, frontend/src/components/ui/button.tsx, card.tsx, input.tsx, textarea.tsx, select.tsx, switch.tsx, checkbox.tsx, badge.tsx, avatar.tsx, tooltip.tsx, dropdown-menu.tsx, dialog.tsx, tabs.tsx, skeleton.tsx, toast.tsx, sheet.tsx
- Tests: No automated tests for this story (UI primitives only). Gate checks: tsc --noEmit PASS, ruff PASS, docker compose build PASS (gateway/core/ai).
- Learnings for future iterations:
  - All 16 shadcn/ui components follow copy-paste pattern (not npm install)
  - Radix UI packages needed: @radix-ui/react-checkbox, react-select, react-switch, react-tabs, react-tooltip, react-avatar (dialog, dropdown-menu, slot already existed)
  - Button uses asChild pattern via @radix-ui/react-slot for polymorphic rendering
  - Badge has 8 custom variants: 5 idea states (open, in_review, accepted, dropped, rejected) + 3 roles (user, reviewer, admin)
  - Avatar has getInitials() helper and hashToHue() for deterministic background colors from user ID
  - Toast is a custom component (not Radix Toast) with 4 semantic variants: success, error, warning, info
  - Sheet reuses @radix-ui/react-dialog (same as Dialog) with slide-in animations per side
  - Tailwind v4 uses @theme directive and CSS custom properties — components reference design token class names (bg-surface, text-foreground, etc.)
  - Frontend Dockerfile build fails (pre-existing nginx.conf issue) — build gateway/core/ai individually
  - docker-compose.yml is at infra/docker/docker-compose.yml, not project root
---
## US-008: Layout Components
- Status: PASS
- Attempts: 1
- Changes: frontend/src/hooks/use-auth.ts, frontend/src/hooks/use-auth-provider.ts, frontend/src/components/layout/Navbar.tsx, NavbarLink.tsx, UserDropdown.tsx, PageShell.tsx, AuthenticatedLayout.tsx, ConnectionIndicator.tsx, frontend/src/components/auth/DevUserSwitcher.tsx, frontend/src/app/providers.tsx
- Tests: No automated tests for this story (UI layout only). Gate checks: tsc --noEmit PASS, ruff PASS, docker compose build PASS (gateway/core/ai).
- Learnings for future iterations:
  - Created useAuth hook + AuthContext for auth state — future stories can use useAuth() for user info, roles, and isAuthenticated
  - AuthProvider wired into providers.tsx using createElement to avoid JSX in .tsx with context
  - useAuthProvider hook manages state; AuthContext is the React context; useAuth is the consumer hook
  - NavbarLink uses react-router-dom NavLink with isActive callback for gold underline (border-primary)
  - Navbar is 56px (h-14), bg-[#002E3C] light / bg-[#0F1A2E] dark, role-gated links via hasRole()
  - UserDropdown uses Radix DropdownMenu from ui/ primitives — theme toggle and language toggle placeholders
  - ConnectionIndicator is a simple green dot + "Online" text placeholder
  - DevUserSwitcher reads VITE_AUTH_BYPASS env var and fetches /auth/dev-users (with hardcoded fallback)
  - AuthenticatedLayout uses Navigate to /login if not authenticated and not in dev bypass mode
  - PageShell wraps Navbar (sticky via sticky top-0) + main content
  - docker-compose.yml is at infra/docker/docker-compose.yml, not project root
---
## US-009: Theme System and i18n Setup
- Status: PASS
- Attempts: 1
- Changes: frontend/src/app/globals.css, frontend/src/app/providers.tsx, frontend/src/components/auth/DevUserSwitcher.tsx, frontend/src/components/layout/ConnectionIndicator.tsx, frontend/src/components/layout/Navbar.tsx, frontend/src/components/layout/UserDropdown.tsx, frontend/src/i18n/config.ts, frontend/src/i18n/locales/de.json, frontend/src/i18n/locales/en.json
- Tests: No automated tests for this story (theme/i18n setup). Gate checks: tsc --noEmit PASS, ruff PASS, docker compose build PASS (gateway/core/ai).
- Learnings for future iterations:
  - CSS variables follow shadcn/ui convention: :root for light, .dark class for dark mode
  - Tailwind v4 @theme directive maps CSS custom properties to utility classes (--color-X becomes bg-X, text-X)
  - Additional design tokens (--surface, --text-primary, --text-secondary, --border-strong, etc.) added beyond shadcn defaults
  - use-theme.ts already existed from US-008 — toggles 'dark' class on <html> and persists to localStorage
  - i18n config uses localStorage.getItem("language") ?? "de" for default German
  - i18next and react-i18next were already in package.json dependencies from initial scaffold
  - Translation files moved from frontend/src/i18n/*.json to frontend/src/i18n/locales/*.json per project structure spec
  - All hardcoded UI text in Navbar, UserDropdown, ConnectionIndicator, DevUserSwitcher replaced with t() calls
  - Language toggle in UserDropdown calls i18n.changeLanguage() and persists to localStorage
  - Theme toggle was already wired in UserDropdown from US-008; now uses translation keys for labels
---
## US-010: Common Components
- Status: PASS
- Attempts: 1
- Changes: frontend/src/components/common/ErrorBoundary.tsx, EmptyState.tsx, ErrorToast.tsx, ErrorLogModal.tsx, LoadingSpinner.tsx, SkipLink.tsx
- Tests: No automated tests for this story (UI components only). Gate checks: tsc --noEmit PASS, ruff PASS, docker compose build PASS (gateway/core/ai).
- Learnings for future iterations:
  - ErrorBoundary is a class component (React error boundaries require getDerivedStateFromError lifecycle)
  - EmptyState accepts a LucideIcon type for the icon prop — import type { LucideIcon } from "lucide-react"
  - ErrorToast/ErrorLogModal are shell components — UI only, no backend wiring; full integration in later milestones
  - LoadingSpinner uses motion-safe:animate-spin to respect prefers-reduced-motion (Tailwind v4 built-in modifier)
  - SkipLink uses sr-only + focus:not-sr-only pattern for accessibility — becomes visible only on keyboard focus
  - All components follow existing codebase patterns: cn() for class merging, design token classes (text-foreground, bg-surface, etc.)
---

## US-001: Database Schema and pgvector Setup (Bugfix Cycle 1)
- Status: PASS
- Attempts: 1
- Changes: No code changes needed — QA confirmed PASS; re-verified gate checks
- Tests: ruff PASS, tsc --noEmit PASS, docker compose build PASS (gateway/core/ai)
- Learnings for future iterations:
  - US-001 was already passing QA — PRD was reset to passes:false for all stories in bugfix mode
  - All 13 migration files (0001_initial.py) confirmed present across gateway/core/ai apps
  - pgvector VectorField + HnswIndex confirmed in services/ai/apps/embedding/models.py
  - docker-compose.yml is at infra/docker/docker-compose.yml (not project root)
---

## US-002: Seed Data (Bugfix Cycle 1)
- Status: PASS
- Attempts: 1
- Changes: No code changes needed — QA confirmed PASS; re-verified gate checks
- Tests: ruff PASS, tsc --noEmit PASS, docker compose build PASS (gateway/core/ai)
- Learnings for future iterations:
  - US-002 was already passing QA — PRD was reset to passes:false for all stories in bugfix mode
  - All 3 seed migrations confirmed: 0002_seed_parameters.py, 0002_seed_buckets.py, 0002_seed_dev_users.py
  - 21 admin parameters (10 application, 3 infrastructure, 8 AI) confirmed in seed migration
  - .ralph/prd.json is a symlink to tasks/prd-m1.json — commit the real file not the symlink
---

## US-003: Gateway Django Models (Bugfix Cycle 1)
- Status: PASS
- Attempts: 1
- Changes: No code changes needed — QA confirmed PASS; re-verified gate checks
- Tests: ruff PASS, tsc --noEmit PASS
- Learnings for future iterations:
  - US-003 was already passing QA — PRD was reset to passes:false for all stories in bugfix mode
  - All 3 gateway models confirmed correct: User (uuid PK, ArrayField roles, JSONField prefs), Notification, MonitoringAlertConfig
---

## US-004: Core Django Models (Bugfix Cycle 1)
- Status: PASS
- Attempts: 1
- Changes: No code changes needed — QA confirmed PASS; re-verified gate checks
- Tests: ruff PASS, tsc --noEmit PASS
- Learnings for future iterations:
  - US-004 was already passing QA — PRD was reset to passes:false for all stories in bugfix mode
  - All 14 core models confirmed: Idea, IdeaCollaborator, ChatMessage, BoardNode, BrdDraft, BrdVersion, ReviewAssignment, ReviewTimelineEntry, CollaborationInvitation, IdeaKeywords, MergeRequest, AdminParameter, AiReaction, UserReaction
  - Immutable save() overrides confirmed on ChatMessage, BrdVersion, ReviewTimelineEntry
  - UniqueConstraint with condition confirmed on ReviewAssignment and MergeRequest
---

## US-005: Authentication Middleware and Dev Bypass (Bugfix Cycle 1)
- Status: PASS
- Attempts: 1
- Changes: services/gateway/gateway/settings/test.py, frontend/src/__tests__/dev-login-flow.test.tsx, .ralph/test-manifest.json
- Tests: 14 Python tests PASS (7 dev bypass + 6 Azure AD + 1 smoke), 5 frontend tests PASS (4 dev login E2E + 1 smoke). Gate checks: ruff PASS, tsc --noEmit PASS, docker compose build PASS (gateway/core/ai).
- Learnings for future iterations:
  - DEF-001: Django test runner prefixes "test_" to DB name. Fix: add TEST.NAME to DATABASES config to use the existing DB name directly
  - DEF-002: T-7.1.04 E2E test uses React Testing Library + Vitest (jsdom) — renders DevUserSwitcher, selects dev user, verifies identity in Navbar
  - Mock fetch with vi.stubGlobal for API calls in component tests
  - AuthContext.Provider used directly in tests to control auth state (isDevBypass, user, setUser)
  - MemoryRouter needed for components that use react-router-dom NavLink
  - Avatar initials (e.g. "AA" for Alice Admin) can be used as assertion target for user identity visibility
---

## US-006: gRPC Service Definitions and Stubs (Bugfix Cycle 1)
- Status: PASS
- Attempts: 1
- Changes: No code changes needed — QA confirmed PASS; re-verified gate checks
- Tests: ruff PASS, tsc --noEmit PASS, docker compose build PASS (gateway/core/ai)
- Learnings for future iterations:
  - US-006 was already passing QA — PRD was reset to passes:false for all stories in bugfix mode
  - All 5 proto files confirmed: common.proto, core.proto, ai.proto, gateway.proto, pdf.proto
  - Generated _pb2.py, _pb2_grpc.py, _pb2.pyi files all present in proto/
  - Stub servicers confirmed in core, gateway, ai (processing + context), pdf
  - gRPC clients confirmed in gateway (core, ai, pdf), ai (core), notification (gateway, core)
  - .ralph/prd.json is a symlink to tasks/prd-m1.json — commit the real file
---

## US-007: UI Primitive Components (Bugfix Cycle 1)
- Status: PASS
- Attempts: 1
- Changes: No code changes needed — QA confirmed PASS; re-verified gate checks
- Tests: tsc --noEmit PASS, ruff PASS
- Learnings for future iterations:
  - US-007 was already passing QA — PRD was reset to passes:false for all stories in bugfix mode
  - All 16 shadcn/ui components confirmed in frontend/src/components/ui/
  - Components: button, card, input, textarea, select, switch, checkbox, badge, avatar, tooltip, dropdown-menu, dialog, tabs, skeleton, toast, sheet
---

## US-008: Layout Components (Bugfix Cycle 1)
- Status: PASS
- Attempts: 1
- Changes: No code changes needed — QA confirmed PASS; re-verified gate checks
- Tests: tsc --noEmit PASS, ruff PASS
- Learnings for future iterations:
  - US-008 was already passing QA — PRD was reset to passes:false for all stories in bugfix mode
  - All 7 layout files confirmed: Navbar.tsx, NavbarLink.tsx, UserDropdown.tsx, PageShell.tsx, AuthenticatedLayout.tsx, ConnectionIndicator.tsx, DevUserSwitcher.tsx
  - useAuth hook + AuthProvider confirmed in hooks/use-auth.ts and use-auth-provider.ts
  - Navbar: 56px height, role-gated links via hasRole(), mobile hamburger menu
  - AuthenticatedLayout: redirects to /login if not authenticated and not in dev bypass mode
  - DevUserSwitcher: visible only when isDevBypass, fetches /auth/dev-users with hardcoded fallback
---

## US-009: Theme System and i18n Setup (Bugfix Cycle 1)
- Status: PASS
- Attempts: 1
- Changes: No code changes needed — QA confirmed PASS; re-verified gate checks
- Tests: tsc --noEmit PASS, ruff PASS
- Learnings for future iterations:
  - US-009 was already passing QA — PRD was reset to passes:false for all stories in bugfix mode
  - CSS variables confirmed: :root (light) + .dark class with full token set (background, foreground, card, primary, secondary, muted, accent, destructive, border, input, ring, surface, text-primary, text-secondary, border-strong)
  - use-theme.ts: reads localStorage, falls back to prefers-color-scheme, toggles .dark class on <html>
  - i18n config: de (German) as default language, localStorage persistence via localStorage.getItem("language") ?? "de"
  - Translation keys cover nav, user menu, common, and dev sections in both de.json and en.json
  - .ralph/prd.json is a symlink to tasks/prd-m1.json — commit the real file
---

## US-010: Common Components (Bugfix Cycle 1)
- Status: PASS
- Attempts: 1
- Changes: No code changes needed — all 6 components already correctly implemented from Phase 2
- Tests: tsc --noEmit PASS, ruff PASS
- Learnings for future iterations:
  - US-010 was already passing QA — PRD was reset to passes:false for all stories in bugfix mode
  - All 6 common components confirmed: ErrorBoundary.tsx, EmptyState.tsx, ErrorToast.tsx, ErrorLogModal.tsx, LoadingSpinner.tsx, SkipLink.tsx
  - ErrorBoundary is a class component (required for getDerivedStateFromError lifecycle)
  - EmptyState uses LucideIcon type for icon prop
  - ErrorToast/ErrorLogModal are shell components — UI only, no backend wiring
  - LoadingSpinner uses motion-safe:animate-spin (Tailwind v4 modifier) to respect prefers-reduced-motion
  - SkipLink uses sr-only + focus:not-sr-only pattern for keyboard-only visibility
  - ruff check services/ fails with E902 — use individual service dirs: services/gateway/ services/core/ services/ai/
---

## US-005: Authentication Middleware and Dev Bypass (Bugfix Cycle 2)
- Status: PASS
- Attempts: 1
- Changes: services/gateway/gateway/settings/test.py
- Tests: 14 Python tests PASS (7 dev bypass + 6 Azure AD + 1 smoke), 5 Node tests PASS (4 dev login E2E + 1 smoke). Gate checks: ruff PASS, tsc --noEmit PASS, docker compose build PASS (gateway/core/ai).
- Learnings for future iterations:
  - DEF-001 root cause: TEST.NAME = NAME = 'ziqreq_test' made Django reuse the live DB as the test DB, causing it to be dropped/recreated between test classes
  - Fix: Remove the TEST block entirely from test settings; let Django auto-create test_ziqreq_test (prefixed with test_)
  - testuser in docker-compose.test.yml is POSTGRES_USER (superuser) so it has CREATE DATABASE privileges for the auto-created test DB
---

## US-005: Authentication Middleware and Dev Bypass (Bugfix Cycle 3)
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/authentication/tests/test_dev_bypass.py, services/gateway/apps/authentication/tests/test_azure_ad.py
- Tests: 14 Python tests PASS (7 dev bypass + 6 Azure AD + 1 smoke), 5 Node tests PASS (4 dev login E2E + 1 smoke). Gate checks: ruff PASS, tsc --noEmit PASS, docker compose build PASS (gateway/core/ai).
- Learnings for future iterations:
  - DEF-001 root cause: @pytest.mark.django_db on django.test.TestCase subclasses creates a pytest-django/TestCase lifecycle conflict — both try to manage DB transactions, causing test DB to be dropped between test classes
  - Fix: Remove @pytest.mark.django_db decorator from TestCase subclasses — Django's TestCase handles DB access natively with pytest-django
  - Also remove unused `import pytest` after removing the decorator
  - django.test.TestCase already wraps each test in a transaction that rolls back — no need for pytest-django's django_db marker
---

## US-005: Authentication Middleware and Dev Bypass (Bugfix Cycle 4)
- Status: PASS
- Attempts: 1
- Changes: No code changes needed — all tests already passing from previous bugfix cycles
- Tests: 14 Python tests PASS (7 dev bypass + 6 Azure AD + 1 smoke), 5 Node tests PASS (4 dev login E2E + 1 smoke). Gate checks: ruff PASS, tsc --noEmit PASS, docker compose build PASS (gateway/core/ai).
- Learnings for future iterations:
  - Previous bugfix cycles (2 and 3) already resolved the DEF-001 issue
  - Key fixes were: removing TEST.NAME override in test.py, removing @pytest.mark.django_db from TestCase subclasses
  - django.test.TestCase handles DB lifecycle natively — don't mix with pytest-django's django_db marker
---

## BF-001: Fix: Test database dropped between test classes (DEF-001)
- Status: PASS
- Attempts: 1
- Changes: conftest.py, services/gateway/gateway/settings/test.py
- Tests: 14 Python tests PASS (7 dev bypass + 6 Azure AD + 1 smoke), 5 Node tests PASS (4 dev login E2E + 1 smoke). Gate checks: ruff PASS, tsc --noEmit PASS, docker compose build PASS (gateway/core/ai).
- Learnings for future iterations:
  - Root cause: conftest.py lacked a django_db_setup fixture override, so pytest-django's default fixture would create/destroy test_ziqreq_test between test classes
  - Fix part 1: Add django_db_setup fixture (scope=session) to conftest.py that runs migrate --run-syncdb on the pre-existing Docker DB
  - Fix part 2: Add TEST dict with NAME, MIRROR, CHARSET, COLLATION, MIGRATE keys to DATABASES config in test.py — Django TestCase._databases_names requires TEST.MIRROR to exist
  - CRITICAL: Docker test images copy files at build time (COPY . .) — changes to settings files require rebuilding python-tests image (docker compose -f docker-compose.test.yml build python-tests)
  - Previous bugfix cycles failed because they didn't rebuild the Docker image after changing test.py
  - Django 5.2's ConnectionHandler.configure_settings uses setdefault to add TEST defaults but won't override existing keys
---

## US-001: Ideas REST API — CRUD Endpoints
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/ideas/models.py, services/gateway/apps/ideas/migrations/__init__.py, services/gateway/apps/ideas/migrations/0001_initial.py, services/gateway/apps/ideas/authentication.py, services/gateway/apps/ideas/serializers.py, services/gateway/apps/ideas/views.py, services/gateway/apps/ideas/urls.py, services/gateway/apps/ideas/tests/__init__.py, services/gateway/apps/ideas/tests/test_views.py, .ralph/test-manifest.json, .ralph/prd.json
- Tests: 31 Python tests PASS (17 ideas CRUD + 13 auth + 1 smoke), 5 Node tests PASS. Gate checks: ruff PASS, tsc --noEmit PASS, docker compose build PASS (gateway/core/ai).
- Learnings for future iterations:
  - Gateway ideas app (label=gateway_ideas) needs its own models for Idea, IdeaCollaborator, ChatMessage since it can't import from core service
  - Models use db_table matching core's tables (ideas, idea_collaborators, chat_messages) — gateway manages tables in test; core manages in production
  - MiddlewareAuthentication bridges middleware auth (request.user_obj) to DRF (request.user) via BaseAuthentication subclass
  - _require_auth must check getattr(user, "id", None) not hasattr(user, "id") — AnonymousUser has id attribute but it's None
  - In dev bypass mode, middleware passes through unauthenticated requests (user_obj=None) without returning 401 — views must handle this
  - DRF api_view decorators should only be on top-level URL-routed functions; inner handlers called from routers should be plain functions
  - Combined GET/POST or GET/DELETE at same URL uses a router function that dispatches by request.method
  - Soft delete uses deleted_at timestamp (not hard delete); trash filter checks deleted_at__isnull=False
  - Search by title uses title__icontains for case-insensitive partial match
  - Collaborating filter joins IdeaCollaborator table; my_ideas filter checks owner_id
  - docker-compose.yml for service builds is at infra/docker/docker-compose.yml
  - Migration line length issues: use module-level constants for choices lists to keep lines under 120 chars
---

## US-002: Invitations List API
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/collaboration/models.py, services/gateway/apps/collaboration/migrations/__init__.py, services/gateway/apps/collaboration/migrations/0001_initial.py, services/gateway/apps/collaboration/apps.py, services/gateway/apps/collaboration/views.py, services/gateway/apps/collaboration/urls.py, services/gateway/apps/collaboration/tests/__init__.py, services/gateway/apps/collaboration/tests/test_views.py, .ralph/test-manifest.json, .ralph/prd.json
- Tests: 36 Python tests PASS (5 invitations + 17 ideas + 13 auth + 1 smoke), 5 Node tests PASS. Gate checks: ruff PASS, tsc --noEmit PASS, docker compose build PASS (gateway/core/ai).
- Learnings for future iterations:
  - Gateway collaboration app needed its own models.py with CollaborationInvitation model (db_table="collaboration_invitations") since it can't import from core service
  - Added label="gateway_collaboration" to CollaborationConfig to avoid potential migration table conflicts with core's collaboration app
  - Reused MiddlewareAuthentication from apps.ideas.authentication — DRF auth bridge pattern works across gateway apps
  - URL route was already wired in gateway/urls.py: path("api/invitations/", include("apps.collaboration.urls"))
  - Serializer not needed — direct dict construction in view is simpler for read-only list endpoints
  - Joins to ideas table (for idea_title) and users table (for inviter display_name) done via batch queries with ID sets to avoid N+1
  - .ralph/ dir is in .gitignore — use git add -f for .ralph/ files
---

## US-003: Landing Page Layout
- Status: PASS
- Attempts: 1
- Changes: frontend/src/pages/LandingPage/index.tsx, frontend/src/components/landing/HeroSection.tsx, frontend/src/components/landing/InvitationCard.tsx, frontend/src/app/router.tsx, frontend/src/i18n/locales/en.json, frontend/src/i18n/locales/de.json, frontend/src/__tests__/landing-page.test.tsx, .ralph/test-manifest.json, .ralph/prd.json
- Tests: 13 Node tests PASS (8 landing page + 4 dev login + 1 smoke). Gate checks: tsc --noEmit PASS, ruff PASS, docker compose build PASS (gateway/core/ai).
- Learnings for future iterations:
  - i18n default language is German ("de") — tests must either use German strings or call i18n.changeLanguage("en") in beforeAll
  - LandingPage accepts props for data (myIdeas, collaborating, invitations, trash) — data fetching hooks will be wired in US-006
  - IdeaCard is a separate US-004 story — LandingPage uses simple placeholder cards for now
  - InvitationCard created but not used in LandingPage yet — will be wired when data fetching is integrated in US-006
  - HeroSection's onSubmit prop is a placeholder for US-005 (idea creation flow)
  - EmptyState component from M1 uses LucideIcon type for icon prop — import icons from lucide-react (Lightbulb, Users, Mail, Trash2)
  - Router updated path from /ideas/:id to /idea/:id to match navigation pattern used in cards
  - PageShell wraps Navbar + main content — LandingPage includes PageShell directly
  - Section component is local to LandingPage — renders heading with count badge
  - Count badge uses rounded-full bg-muted pill style
  - vi.mock("react-router-dom") with useNavigate mock needed for testing card click navigation
---

## US-004: IdeaCard Component
- Status: PASS
- Attempts: 1
- Changes: frontend/src/components/landing/IdeaCard.tsx, frontend/src/lib/utils.ts, frontend/src/pages/LandingPage/index.tsx, frontend/src/i18n/locales/en.json, frontend/src/i18n/locales/de.json, frontend/src/__tests__/idea-card.test.tsx, frontend/src/__tests__/landing-page.test.tsx
- Tests: 23 Node tests PASS (10 IdeaCard + 8 landing page + 4 dev login + 1 smoke). Gate checks: tsc --noEmit PASS, ruff PASS, docker compose build PASS (gateway/core/ai).
- Learnings for future iterations:
  - IdeaCard uses inline style for state dot color (dynamic per state) rather than Tailwind classes
  - State dot colors: open=#0284C7, in_review=#F59E0B, accepted=#16A34A, dropped=#9CA3AF, rejected=#F97316
  - formatRelativeTime helper added to lib/utils.ts — simple logic: <1min="just now", <1hr="X minutes ago", <1day="X hours ago", else="X days ago"
  - Three-dot menu uses stopPropagation on both onClick and onKeyDown to prevent card navigation
  - DropdownMenuTrigger wraps a span (not button) with role="button" — avoids nested button issue inside card's button element
  - Badge component already has state variants (open, in_review, accepted, dropped, rejected) from M1
  - LandingPage invitations section must use InvitationCard (not IdeaCard) — invitations have different data shape
  - InvitationCard renders "From {{name}}" via i18n, not raw inviter name — tests must match this format
  - Docker test images copy files at build time — must rebuild node-tests after source changes
  - i18n keys added under landing.ideaCard namespace: lastUpdated, delete, restore
---

## US-005: Idea Creation Flow
- Status: PASS
- Attempts: 1
- Changes: frontend/src/api/ideas.ts (new), frontend/src/hooks/use-create-idea.ts (new), frontend/src/components/landing/HeroSection.tsx, frontend/src/i18n/locales/en.json, frontend/src/i18n/locales/de.json, frontend/src/__tests__/idea-creation-flow.test.tsx (new), frontend/src/__tests__/landing-page.test.tsx, .ralph/test-manifest.json, .ralph/prd.json
- Tests: 29 Node tests PASS (6 idea creation + 10 IdeaCard + 8 landing page + 4 dev login + 1 smoke). Gate checks: tsc --noEmit PASS, ruff PASS, docker compose build PASS (gateway/core/ai).
- Learnings for future iterations:
  - HeroSection now self-contained — uses useCreateIdea hook internally instead of accepting onSubmit prop
  - API layer created at frontend/src/api/ideas.ts — uses fetch with credentials: "include" for session auth
  - useCreateIdea hook wraps TanStack Query useMutation with onSuccess redirect via useNavigate
  - Textarea used instead of Input for hero section (allows multi-line, min-h-24)
  - Enter key submits (Shift+Enter for newline) via onKeyDown with e.shiftKey check
  - Validation error state managed locally (not via mutation) — cleared on typing
  - Error state from mutation rendered inline with retry button (not a separate toast component)
  - QueryClientProvider needed in test wrappers when components use TanStack Query hooks
  - vi.stubGlobal("fetch", mockFetch) used to mock API calls in component tests
  - i18n keys added: landing.hero.validationEmpty, landing.hero.errorCreating
---

## US-006: Idea Lists with TanStack Query
- Status: PASS
- Attempts: 1
- Changes: frontend/src/api/ideas.ts, frontend/src/hooks/use-my-ideas.ts (new), use-collaborating-ideas.ts (new), use-invitations.ts (new), use-trash.ts (new), use-delete-idea.ts (new), use-restore-idea.ts (new), use-create-idea.ts, frontend/src/components/landing/IdeaCardSkeleton.tsx (new), frontend/src/pages/LandingPage/index.tsx, frontend/src/app/providers.tsx, frontend/src/i18n/locales/en.json, frontend/src/i18n/locales/de.json, frontend/src/__tests__/landing-page.test.tsx, frontend/src/__tests__/idea-creation-flow.test.tsx
- Tests: 31 Node tests PASS (10 landing page + 10 IdeaCard + 6 idea creation + 4 dev login + 1 smoke). Gate checks: tsc --noEmit PASS, ruff PASS, docker compose build PASS (gateway/core/ai).
- Learnings for future iterations:
  - LandingPage no longer accepts props — it uses TanStack Query hooks internally for all 4 lists
  - API response fields use snake_case (updated_at, deleted_at, idea_id) not camelCase — map accordingly in components
  - react-toastify is in package.json but requires ToastContainer in providers.tsx to work
  - ToastContainer added to Providers with position="bottom-right"
  - Test mocking pattern for query hooks: use `vi.mock("@/hooks/use-xxx")` at module level, import mocked module, use `vi.mocked(hook).mockReturnValue()` per test
  - Cast mock return values as `never` to bypass TanStack Query's complex UseQueryResult type in tests
  - When LandingPage switches from prop-based to hook-based, ALL test files rendering LandingPage must mock the list hooks (including idea-creation-flow tests)
  - useDeleteIdea/useRestoreIdea invalidate queryKey: ["ideas"] on success to refresh all idea lists
  - useCreateIdea also invalidates ["ideas"] queries before navigating
  - Undo toast uses react-toastify's toast() with JSX content containing an undo button; 5s autoClose
  - docker-compose.yml is at infra/docker/docker-compose.yml — use that for service builds
---

## US-007: Search & Filter Bar
- Status: PASS
- Attempts: 1
- Changes: frontend/src/components/landing/FilterBar.tsx (new), frontend/src/hooks/use-ideas-filters.ts (new), frontend/src/hooks/use-my-ideas.ts, frontend/src/hooks/use-collaborating-ideas.ts, frontend/src/pages/LandingPage/index.tsx, frontend/src/i18n/locales/en.json, frontend/src/i18n/locales/de.json, frontend/src/__tests__/filter-bar.test.tsx (new)
- Tests: 44 Node tests PASS (13 filter-bar + 10 landing page + 10 IdeaCard + 6 idea creation + 4 dev login + 1 smoke), 36 Python tests PASS. Gate checks: tsc --noEmit PASS, ruff PASS, docker compose build PASS (gateway/core/ai).
- Learnings for future iterations:
  - setSearchParams from useSearchParams creates a new function reference on every render — use a ref to avoid infinite re-render loops in useEffect dependencies
  - useIdeasFilters hook uses refs (searchParamsRef, setSearchParamsRef) to break the setSearchParams → re-render → new setSearchParams → effect re-fires cycle
  - userEvent.type triggers re-renders between keystrokes and waits for DOM to settle — this causes timeouts when combined with setSearchParams/MemoryRouter interactions
  - Use fireEvent.change instead of userEvent.type for search input tests that involve URL params/debounce
  - Test FilterBar as a controlled component (receiving props) for unit tests — avoids the useSearchParams/MemoryRouter timing issues
  - Debounce effect (300ms) only depends on searchInput (not setSearchParams) — key to avoiding infinite loops
  - Radix Select doesn't support empty string as value — use a sentinel value like "__all__" and map to "" in the handler
  - FilterBar filters only apply to My Ideas and Collaborating lists — Invitations and Trash are unfiltered
  - Ownership filter determines which lists to show: "my_ideas" → only My Ideas, "collaborating" → only Collaborating, "" → both
  - i18n keys added under landing.filter namespace for search, state, ownership labels
---

## US-008: Ideas List Floating Window
- Status: PASS
- Attempts: 1
- Changes: frontend/src/components/landing/IdeaCardCompact.tsx (new), frontend/src/components/layout/IdeasListFloating.tsx (new), frontend/src/hooks/use-ideas-by-state.ts (new), frontend/src/components/layout/Navbar.tsx, frontend/src/i18n/locales/en.json, frontend/src/i18n/locales/de.json, frontend/src/__tests__/ideas-list-floating.test.tsx (new)
- Tests: 52 Node tests PASS (8 floating window + 13 filter-bar + 10 landing page + 10 IdeaCard + 6 idea creation + 4 dev login + 1 smoke), 36 Python tests PASS. Gate checks: tsc --noEmit PASS, ruff PASS, docker compose build PASS (gateway/core/ai).
- Learnings for future iterations:
  - No Popover component from M1 — used custom click-outside + Escape handling with useRef/useEffect instead of Radix Popover
  - IdeasListFloating uses absolute positioning (z-20) anchored relative to its parent wrapper in the Navbar
  - IdeaCardCompact is minimal: state dot + title only (no timestamp, badge, or menu) — separate from full IdeaCard
  - useIdeasByState hook uses fetchIdeas with state param — Closed tab maps to state=dropped (not "closed")
  - Tabs component from M1 (Radix Tabs) works well for the tabbed interface
  - useCallback on onClose handler prevents unnecessary re-renders of IdeasListFloating
  - i18n keys added under ideasFloat namespace (separate from landing namespace)
  - Tests mock useIdeasByState at module level and verify tab rendering, navigation, and close behaviors
  - mousedown event (not click) used for click-outside detection to match standard popover behavior
---

## US-001: Chat Messages API
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/chat/serializers.py, services/gateway/apps/chat/views.py, services/gateway/apps/chat/urls.py, services/gateway/apps/ideas/urls.py, services/gateway/apps/chat/tests/__init__.py, services/gateway/apps/chat/tests/test_chat_messages.py, .ralph/prd.json
- Tests: 51 Python tests PASS (15 new chat message tests + 36 existing), 52 Node tests PASS. Gate checks: ruff PASS, tsc --noEmit PASS, docker compose build PASS (gateway/core/ai).
- Learnings for future iterations:
  - ChatMessage model already exists in apps.ideas.models (gateway mirror) — chat views import from there, no separate model needed
  - Chat URL route wired via ideas urls.py: path("<str:idea_id>/chat", include("apps.chat.urls")) — nested under /api/ideas/:id/
  - Reused MiddlewareAuthentication from apps.ideas.authentication for DRF auth bridge
  - Access check pattern: owner_id, co_owner_id, or IdeaCollaborator lookup — extracted to _check_access helper
  - Pagination uses limit/offset (not page/page_size like ideas list) — matches spec for chat
  - ChatMessage is immutable (append-only) — create only, no update/delete endpoints
  - Deleted ideas (deleted_at not null) return 404 for chat endpoints
  - docker-compose.yml is at infra/docker/docker-compose.yml for service builds
---

## US-002: User Reactions API
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/ideas/models.py, services/gateway/apps/ideas/migrations/0002_userreaction.py, services/gateway/apps/chat/serializers.py, services/gateway/apps/chat/views.py, services/gateway/apps/chat/urls.py, services/gateway/apps/chat/tests/test_reactions.py, .ralph/test-manifest.json, .ralph/prd.json
- Tests: 63 Python tests PASS (12 new reaction tests + 51 existing), 52 Node tests PASS. Gate checks: ruff PASS, tsc --noEmit PASS, docker compose build PASS (gateway/core/ai).
- Learnings for future iterations:
  - UserReaction model added to apps.ideas.models (same file as ChatMessage mirror model) with db_table="user_reactions"
  - Needed a migration (0002_userreaction.py) for the test DB — without it, relation "user_reactions" does not exist
  - Docker test images copy files at build time — must rebuild python-tests image after adding migration files
  - Reaction URL nested under chat: chat/urls.py has path("/<str:message_id>/reactions", ...) which resolves to /api/ideas/:id/chat/:msgId/reactions
  - UNIQUE(message_id, user_id) constraint enforces one reaction per user per message — application-level check before create avoids IntegrityError
  - Validation order: auth → idea access → message exists → not AI message → not own message → valid reaction_type → not duplicate
  - DELETE doesn't need message validation — just find reaction by message_id + user_id and delete
  - .ralph/ dir is in .gitignore — use git add -f for .ralph/ files
---

## US-003: Workspace Page Route
- Status: PASS
- Attempts: 1
- Changes: frontend/src/pages/IdeaWorkspace/index.tsx, frontend/src/pages/idea-workspace.tsx, frontend/src/app/router.tsx, frontend/src/api/ideas.ts, frontend/src/i18n/locales/en.json, frontend/src/i18n/locales/de.json
- Tests: 63 Python tests PASS, 52 Node tests PASS. Gate checks: tsc --noEmit PASS, ruff PASS, docker compose build PASS (gateway/core/ai).
- Learnings for future iterations:
  - IdeaWorkspacePage replaces placeholder div in router — uses useParams to extract :id
  - fetchIdea function added to api/ideas.ts with Idea interface matching GET /api/ideas/:id response
  - Error object includes .status property for distinguishing 404/403 from generic errors
  - document.title updated via useEffect on idea.title, reset to "ZiqReq" on unmount
  - Loading state uses Skeleton components for header and two-panel layout preview
  - i18n keys added under "workspace" namespace: notFound, noAccess, backToHome
  - ruff check services/ fails with E902 — use individual dirs: services/gateway/ services/core/ services/ai/
---

## US-004: Two-Panel Layout with Draggable Divider
- Status: PASS
- Attempts: 1
- Changes: frontend/src/components/workspace/WorkspaceLayout.tsx (new), frontend/src/components/workspace/PanelDivider.tsx (new), frontend/src/pages/IdeaWorkspace/index.tsx
- Tests: 63 Python tests PASS, 52 Node tests PASS. Gate checks: tsc --noEmit PASS, ruff PASS, docker compose build PASS (gateway/core/ai).
- Learnings for future iterations:
  - WorkspaceLayout uses ratio (0-1) stored in localStorage (key: workspace-panel-split), not pixels
  - PanelDivider uses 4px visual width with 4px padding on each side for 12px hit area
  - Document-level mousemove/mouseup listeners attached only while dragging (isDragging state)
  - clampRatio ensures min 280px chat and 320px board based on container width
  - Double-click resets to 40/60 default with 200ms CSS transition
  - Tabs component from M1 (Radix Tabs) with defaultValue="board" makes Board tab active by default
  - WorkspaceLayout accepts optional chatPanel prop for future chat content injection
  - PanelDivider uses bg-primary (gold) on hover/drag with shadow glow effect
---

## US-005: Workspace Header
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/ideas/views.py, services/gateway/apps/ideas/serializers.py, frontend/src/api/ideas.ts, frontend/src/components/workspace/WorkspaceHeader.tsx (new), frontend/src/pages/IdeaWorkspace/index.tsx
- Tests: Python 63 passed, Node 52 passed, tsc --noEmit clean, ruff clean
- Learnings for future iterations:
  - Added PATCH /api/ideas/:id endpoint supporting title and agent_mode updates
  - IdeaPatchSerializer validates at least one field provided, agent_mode choices
  - Backend sets title_manually_edited=true when title field is in PATCH request
  - WorkspaceHeader uses optimistic updates with rollback on error for both title and agent_mode
  - Title editing: click to enter edit mode, Enter saves, Escape cancels, blur saves
  - Agent mode uses shadcn Select component with onValueChange
  - Pre-existing Docker build failures (pdf service apt-get, frontend nginx.conf) unrelated to changes
  - ideas_detail view now supports GET, PATCH, DELETE methods
---

## US-006: Section Visibility + Locking
- Status: PASS
- Attempts: 1
- Changes: frontend/src/components/workspace/useSectionVisibility.ts (new), frontend/src/components/workspace/LockOverlay.tsx (new), frontend/src/components/workspace/ChatPanel.tsx (new), frontend/src/components/workspace/WorkspaceLayout.tsx (modified), frontend/src/components/workspace/WorkspaceHeader.tsx (modified), frontend/src/pages/IdeaWorkspace/index.tsx (modified)
- Tests: 63 Python passed, 52 Node passed, tsc --noEmit passed, ruff passed, docker build (gateway/core/ai) passed
- Learnings for future iterations:
  - useSectionVisibility hook derives all visibility/lock state from idea.state using M3 heuristic: has_been_submitted = state !== 'open'
  - LockOverlay uses absolute positioning with bg-card/80 backdrop, Lock icon from lucide-react
  - ChatPanel is a thin wrapper that shows LockOverlay when locked
  - WorkspaceLayout accepts reviewVisible prop to conditionally render the Review tab trigger and content
  - WorkspaceHeader accepts readOnly prop to disable title editing and agent mode dropdown in terminal states (accepted/dropped)
  - IdeaWorkspaceContent extracted as inner component to allow calling useSectionVisibility hook (hooks can't be conditional)
  - Pre-existing Docker build failures (frontend nginx.conf, pdf service) still present — build gateway/core/ai individually
---

## US-007: Chat Message Display
- Status: PASS
- Attempts: 1
- Changes: frontend/src/api/chat.ts (new), frontend/src/components/chat/ChatMessageList.tsx (new), frontend/src/components/chat/UserMessageBubble.tsx (new), frontend/src/components/chat/AIMessageBubble.tsx (new), frontend/src/components/chat/DelegationMessage.tsx (new), frontend/src/components/workspace/ChatPanel.tsx (modified), frontend/src/pages/IdeaWorkspace/index.tsx (modified)
- Tests: 63 Python passed, 52 Node passed, tsc --noEmit passed, ruff passed, docker build (gateway/core/ai) passed
- Learnings for future iterations:
  - Chat API client (api/chat.ts) follows same pattern as api/ideas.ts: fetch with credentials:"include", error handling
  - ChatMessageList fetches messages on mount via fetchChatMessages(idea.id), displays loading spinner then messages or empty state
  - Auto-scroll uses useRef on a div at bottom of message list + scrollIntoView in useEffect watching messages array
  - First scroll (on load) uses behavior:"instant", subsequent scrolls (new messages) use behavior:"smooth" — tracked via prevMessageCountRef
  - Multi-user detection: idea.collaborators.length > 0 — sender name shown below user bubbles for other users' messages only
  - UserMessageBubble: right-aligned (items-end), bg-secondary text-secondary-foreground, rounded-tr-sm for chat bubble shape
  - AIMessageBubble: left-aligned (items-start), bg-card border, rounded-tl-sm, Bot icon + "AI" label above bubble
  - DelegationMessage: same as AI but opacity-60, italic text, lighter border (border-border/50)
  - ChatPanel now requires idea prop — pass from IdeaWorkspaceContent where idea is guaranteed non-null
  - EmptyState component accepts LucideIcon type — used Lightbulb icon for "Start brainstorming..." empty state
  - i18n fallback strings used (t("chat.emptyState", "Start brainstorming...")) — works without explicit i18n keys
---

## US-008: ChatInput with Send Button
- Status: PASS
- Attempts: 1
- Changes: frontend/src/components/chat/ChatInput.tsx (new), frontend/src/api/chat.ts, frontend/src/components/chat/ChatMessageList.tsx, frontend/src/components/workspace/ChatPanel.tsx
- Tests: tsc --noEmit PASS, ruff check PASS. Docker build has pre-existing nginx.conf issue (not related to this story).
- Learnings for future iterations:
  - ChatInput uses auto-grow textarea: rows=1, reset height to "auto" then set to scrollHeight on input
  - sendChatMessage added to api/chat.ts with POST /api/ideas/:id/chat/
  - ChatPanel manages newMessages state and passes appendedMessages to ChatMessageList for deduplication
  - ChatMessageList merges fetched + appended messages using useMemo with Set-based dedup on message id
  - Enter sends (prevented default), Shift+Enter allows newline via no preventDefault
  - Send button uses Button variant="primary" size="icon" with ArrowRight icon, Loader2 spinner when sending
  - Context indicator is a 20px circle placeholder (for M7)
---

## US-009: @Mention Dropdown
- Status: PASS
- Attempts: 1
- Changes: frontend/src/components/chat/MentionDropdown.tsx (new), frontend/src/components/chat/ChatInput.tsx (modified), frontend/src/components/workspace/ChatPanel.tsx (modified)
- Tests: tsc --noEmit PASS, ruff check PASS. Docker build has pre-existing nginx.conf issue (not related to this story).
- Learnings for future iterations:
  - MentionDropdown is a presentational component; all state logic lives in ChatInput
  - ChatInput now accepts optional `idea` prop for collaborator list access
  - Regex `/(^|[\s])@([^\s]*)$/` detects @ trigger at word boundary before cursor
  - Used `requestAnimationFrame` for `setSelectionRange` after React state update to ensure DOM is synced
  - `onMouseDown` with `preventDefault` on dropdown items prevents textarea blur
  - TypeScript strict mode requires explicit undefined checks on array index access (`filteredMentionItems[index]`)
---

## US-010: User Reactions UI
- Status: PASS
- Attempts: 1
- Changes: frontend/src/api/reactions.ts (new), frontend/src/components/chat/ReactionChips.tsx (new), frontend/src/components/chat/UserMessageBubble.tsx (modified), frontend/src/components/chat/ChatMessageList.tsx (modified)
- Tests: 63 Python PASS, 52 Node PASS, tsc --noEmit PASS, ruff PASS, docker build (gateway/core/ai) PASS
- Learnings for future iterations:
  - ReactionChips uses optimistic updates with rollback on error
  - react-toastify toast.error() for showing validation errors (400/409) from the reactions API
  - UserMessageBubble now takes ideaId and isOwnMessage props to conditionally render ReactionChips
  - Reactions are only shown on other users' messages (not own, not AI) per spec
  - Switching reactions requires removing old reaction first then adding new one (UNIQUE constraint on message_id, user_id)
  - Pre-existing Docker build failures (frontend nginx.conf, pdf service) still present — build gateway/core/ai individually
---

## US-003: Workspace Page Route
- Status: PASS
- Attempts: 1
- Changes: frontend/src/pages/IdeaWorkspace/index.tsx, frontend/src/__tests__/idea-workspace.test.tsx
- Tests: 8 test files, 56 tests passed (node). 63 Python tests passed. tsc --noEmit PASS. ruff PASS. Docker build PASS (gateway/core/ai).
- Learnings for future iterations:
  - DEF-002 fix: useCallback must be called before any early returns to comply with rules-of-hooks
  - Test pattern: mock heavy child components (WorkspaceLayout, WorkspaceHeader, ChatPanel) with vi.mock to isolate page-level behavior
  - MemoryRouter with Routes/Route needed to provide useParams context in tests
  - Docker compose build for frontend fails (pre-existing nginx.conf issue) — build gateway/core/ai individually
---

## US-004: Two-Panel Layout with Draggable Divider
- Status: PASS
- Attempts: 1
- Changes: frontend/src/__tests__/workspace-layout.test.tsx (new)
- Tests: 9/9 pass (workspace-layout.test.tsx), 65/65 total frontend tests pass, tsc clean, ruff clean
- Learnings for future iterations:
  - WorkspaceLayout and PanelDivider were already fully implemented in Phase 2; only tests were missing (QA DEF-001)
  - jsdom has 0 container width so clampRatio always returns DEFAULT_RATIO — test localStorage persistence by checking style.width is truthy rather than a specific value
  - Radix Tabs set data-state="active" on the default tab trigger — use that for asserting default tab
  - fireEvent.mouseMove/mouseUp must be dispatched on `document` (not the divider element) since the drag handlers are registered on document
---

## US-005: Workspace Header
- Status: PASS
- Attempts: 1
- Changes: frontend/src/__tests__/workspace-header.test.tsx (new)
- Tests: 13 new tests all passing (T-1.6.01: 5 tests, T-1.6.02: 2 tests, T-1.6.03: 1 test, T-2.1.01: 3 tests, T-2.1.02: 2 tests). Full suite 78/78 passing.
- Learnings for future iterations:
  - WorkspaceHeader component was already fully implemented from Phase 2; only missing tests
  - patchIdea frontend API and backend PATCH endpoint with title_manually_edited already existed
  - Radix Select testing in jsdom requires polyfills: PointerEvent, ResizeObserver, hasPointerCapture, setPointerCapture, releasePointerCapture, scrollIntoView
  - userEvent.setup() needed for Radix Select interaction tests (click trigger then click option)
  - Tests must run from frontend/ directory for @ alias resolution to work
  - Docker compose build has pre-existing nginx.conf issue unrelated to M3 changes
---

## US-006: Section Visibility + Locking
- Status: PASS
- Attempts: 1
- Changes: frontend/src/__tests__/section-visibility.test.tsx (new), .ralph/prd.json
- Tests: 14 new tests (T-1.2.01, T-1.2.02, T-1.2.03, T-1.4.01-05) all passing. 92 total frontend tests pass. tsc --noEmit PASS. ruff check PASS. Docker build PASS (gateway/core/ai).
- Learnings for future iterations:
  - Implementation (useSectionVisibility hook, LockOverlay, ChatPanel integration) was already complete from Phase 2
  - QA flagged only missing tests — the bugfix was adding 14 integration tests covering all state transitions
  - Tests mock ChatMessageList and ChatInput to isolate section visibility behavior
  - The Idea type from @/api/ideas uses snake_case (agent_mode, owner_id) matching backend API
  - Pre-existing Docker build failures (frontend nginx.conf, pdf service) still present — build gateway/core/ai individually
---

## US-009: @Mention Dropdown
- Status: PASS
- Attempts: 1
- Changes: frontend/src/__tests__/mention-dropdown.test.tsx (new)
- Tests: 11 new tests (T-2.9.01: 4 tests, T-2.9.02: 4 tests, T-2.9.03: 3 tests), all 103 frontend tests pass
- Learnings for future iterations:
  - MentionDropdown and ChatInput mention logic were already fully implemented in Phase 2
  - QA only flagged missing tests, not implementation bugs
  - fireEvent.change doesn't set selectionStart on textarea - use Object.defineProperty to set it before triggering change
  - MentionDropdown uses onMouseDown (not onClick) to prevent textarea blur
  - Test data-testid pattern: mention-item-{id} for individual items, mention-dropdown for container
---
