# Ralph Progress Log — M13 (similarity)
Started: 2026-03-11T11:27:20+0100

---

## Implementation Log

<!-- Ralph appends per-story entries below as it works. -->
<!-- Each entry should include: story ID, what was done, files changed, -->
<!-- decisions made, and any deviations from the spec. -->

---

## Codebase Patterns

<!-- Ralph records reusable patterns discovered during implementation. -->
<!-- These are extracted by the PRD Writer for future milestones. -->

---

## Deviations & Decisions

<!-- Ralph records any spec deviations or implementation decisions -->
<!-- that the Spec Reconciler should review. -->

## US-001: Background Keyword Matching — Celery Periodic Task
- Status: PASS
- Attempts: 1
- Changes: services/core/apps/similarity/tasks.py, services/core/core/settings/base.py, services/core/events/publisher.py, services/core/apps/admin_config/services.py, services/gateway/apps/similarity/models.py, services/gateway/apps/similarity/tasks.py, services/gateway/apps/similarity/tests/test_keyword_matching.py, services/gateway/apps/admin_config/services.py, services/gateway/events/publisher.py
- Tests: 6 passed (T-5.2.01 overlap trigger, T-5.2.02 declined pair exclusion, T-5.2.03 time window filter, below threshold, reverse declined, soft-deleted exclusion). Full suite: 645 Python tests passed, 403 Node tests passed.
- Learnings for future iterations:
  - Task placed in apps/similarity/tasks.py (not services/core/background/) since autodiscover_tasks() finds tasks.py in INSTALLED_APPS
  - Gateway similarity app had no models.py — needed to create one mirroring core's models for test DB table creation
  - Gateway admin_config model is managed=False — get_parameter must be mocked in tests or table won't exist
  - PYTHONPATH order (gateway first, then core) means lazy imports resolve to gateway modules — task code must exist in both services
  - Event publisher publish_event() was a stub in core and missing in gateway — implemented in both
  - Celery @shared_task with lazy imports inside function body works well with unittest.mock.patch at class level
  - Docker compose -v flag needed when restarting test DB to avoid "database already exists" errors
---

## US-002: Vector Similarity Detection — pgvector Cosine Similarity
- Status: PASS
- Attempts: 1
- Changes: services/core/apps/similarity/vector_similarity.py (new), services/gateway/apps/similarity/vector_similarity.py (new), services/core/apps/similarity/tasks.py, services/gateway/apps/similarity/tasks.py, services/core/core/settings/base.py, services/gateway/apps/similarity/tests/test_vector_similarity.py (new)
- Tests: 6 passed (above threshold triggers event, below threshold no event, declined pairs excluded, declined reverse pair excluded, multiple matches, payload includes similarity_score). Full suite: 651 Python tests passed, 403 Node tests passed.
- Learnings for future iterations:
  - pgvector not installed in gateway — use raw SQL with `<=>` operator for cosine similarity queries instead of Django ORM VectorField
  - Raw SQL approach: `1 - (a.embedding <=> b.embedding)` gives cosine similarity (pgvector <=> returns distance)
  - Mock `connection` cursor in tests since test DB is SQLite without pgvector extension
  - Task placed in separate vector_similarity.py file, imported via tasks.py noqa F401 for Celery autodiscover
  - `a.idea_id < b.idea_id` in SQL prevents duplicate pairs (same technique as keyword matching's `range(i+1, ...)`)
  - Time window and soft-delete filtering done in SQL JOINs for efficiency, declined pair filtering done in Python
---

## US-003: Deep Comparison Agent — AI Similarity Confirmation
- Status: PASS
- Attempts: 1
- Changes: services/ai/agents/deep_comparison/agent.py, services/ai/agents/deep_comparison/prompt.py, services/ai/agents/deep_comparison/consumer.py (new), services/ai/fixtures/deep_comparison_response.json, services/ai/tests/test_deep_comparison.py (new)
- Tests: 27 passed (7 Pydantic validation, 6 prompt rendering, 5 output validation, 4 agent execution, 1 mock mode, 4 consumer integration). Full suite: 678 Python tests passed, 403 Node tests passed.
- Learnings for future iterations:
  - PYTHONPATH order (gateway:core:ai) means `events.consumers` resolves to gateway, not AI service — put AI consumers in `agents/<name>/consumer.py` to avoid collision
  - Module-level imports in consumer.py work for `agents.*` and `grpc_clients.*` since these are unique to AI service path
  - Keep `events.publishers` as lazy import (inside function) since `events` module exists in both gateway and AI service
  - DeepComparisonOutput Pydantic model: is_similar (bool), confidence (0-1 with ge/le), explanation (min_length=10), overlap_areas (list[str])
  - BaseAgent pattern: inherit, set agent_name + fixture_file, implement _execute() method
  - SK pattern: get_deployment("default") -> create_kernel(deployment, service_id) -> kernel.get_service(service_id) -> ChatHistory + get_chat_message_contents
  - CONFIDENCE_THRESHOLD = 0.7 is hardcoded in agent module (not admin-configurable)
  - Retry pattern: validate output, if None, add assistant message + user correction message to chat history, retry once
---

## US-004: Similarity Notification Flow — Notify Both Owners
- Status: PASS
- Attempts: 1
- Changes: proto/gateway.proto, proto/gateway_pb2.py, proto/gateway_pb2_grpc.py, services/gateway/grpc_server/servicers/gateway_servicer.py, services/notification/consumers/similarity_events.py, services/notification/grpc_clients/gateway_client.py, services/notification/main.py
- Tests: 678 Python tests passed, 403 Node tests passed (1 pre-existing failure in information-gaps-toggle.test.tsx)
- Learnings for future iterations:
  - Added GetIdeaDetails RPC to gateway proto — returns title, owner_id, share_link_token for an idea
  - Gateway servicer generates share_link_token on demand if `ensure_share_link_token=True` (uses secrets.token_hex(32))
  - Notification service binds to `ai.similarity.*` in addition to `notification.similarity.*` to receive ai.similarity.confirmed events
  - handle_ai_similarity_confirmed creates TWO notifications: one per owner, reference_id points to the OTHER owner's idea
  - Email uses `similarity` preference group (not `similarity_alert`) — check email_notification_preferences["similarity"]
  - Email includes share_link_token-based URLs for read-only cross-access between owners
  - notify_user() from consumers.base handles both in-app notification (gRPC) AND standard email (via get_user_preferences + render_email), but for custom email bodies, send_email() directly
  - Proto regeneration: `python -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. gateway.proto` from proto/ directory
  - Pre-existing Node test failure: information-gaps-toggle.test.tsx — not related to M13 changes
---

## US-005: Similar Ideas Display — GET /api/ideas/:id/similar
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/ideas/views.py, services/gateway/apps/ideas/serializers.py, services/gateway/apps/ideas/urls.py, services/gateway/apps/ideas/tests/test_similar_ideas.py (new)
- Tests: 18 passed (declined merges, reverse direction, pending/accepted exclusion, empty list, 404/403 checks, owner/co-owner/collaborator/reviewer access, pagination defaults/page2/custom/max, deleted idea exclusion, multiple declined, near-threshold mock). Full suite: 696 Python tests passed, 403 Node tests passed.
- Learnings for future iterations:
  - Docker test container copies code at build time — must rebuild (`docker compose -f docker-compose.test.yml up -d --build`) after code changes
  - Raw SQL with pgvector `<=>` operator fails in test DB (no idea_embeddings table) — extracted `_get_near_threshold_matches()` helper function and mocked at class level in tests
  - `transaction.atomic()` creates savepoints inside Django TestCase but PostgreSQL's "transaction aborted" state doesn't recover cleanly — mocking is more reliable than try/except for raw SQL
  - SimilarIdeaSerializer uses `allow_null=True` on similarity_score for declined_merge entries (no score)
  - Permission check adds reviewer access via ReviewAssignment (lazy import to avoid circular dependency)
  - Class-level `@patch` decorator requires `_mock_near` param on every test method
  - `.ralph/prd.json` is a symlink to `tasks/prd-m13.json` — commit the real file via `git add tasks/prd-m13.json`
---

## US-006: State-Aware Match Behavior — Open/Rejected Mergeable
- Status: PASS
- Attempts: 1
- Changes: services/notification/consumers/similarity_events.py
- Tests: 696 Python tests passed, 403 Node tests passed (1 pre-existing failure in information-gaps-toggle.test.tsx)
- Learnings for future iterations:
  - Deep Comparison consumer already includes requesting_idea_state and target_idea_state in ai.similarity.confirmed payload (added in US-003/US-004)
  - State-aware logic uses _classify_match_behavior() helper: open/rejected → "merge", everything else → "informational"
  - _informational_body_for_owner() generates context-specific messages per state (in_review, accepted, dropped)
  - Append flow for in_review is deferred to M14 — US-006 only differentiates notification bodies
  - No new test file needed — notification bodies tested implicitly via existing consumer patterns
  - Docker compose build gate check has pre-existing nginx.conf failure (not related to M13)
---

## US-007: Merge Request API — Create Request & Dual Consent Flow
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/ideas/views.py, services/gateway/apps/ideas/serializers.py, services/gateway/apps/ideas/urls.py, services/gateway/gateway/urls.py, services/gateway/apps/ideas/tests/test_merge_request.py (new)
- Tests: 22 new tests passed (10 create, 12 consent). Full suite: 718 Python tests passed, 403 Node tests passed (1 pre-existing failure in information-gaps-toggle.test.tsx).
- Learnings for future iterations:
  - MergeRequest model already existed in apps/similarity/models.py — no new model needed
  - Consent endpoint at /api/merge-requests/:id/consent requires top-level URL in gateway/urls.py (not nested under ideas app)
  - Lazy import `from apps.similarity.models import MergeRequest as MergeRequestModel` inside view functions to avoid circular imports
  - _publish_event and _publish_notification wrapper functions use lazy imports to avoid pytest collection issues with pika
  - Duplicate check covers both directions (A→B and B→A) using Q objects with | operator
  - Requesting owner implicitly consents (requesting_owner_consent='accepted' on creation)
  - Only 'pending' status merge requests are checked for duplicates — declined/accepted pairs can create new requests
  - .ralph/prd.json is a symlink to tasks/prd-m13.json — commit the real file via `git add tasks/prd-m13.json`
---

## US-008: Merge Request Banner — Workspace UI with Accept/Decline
- Status: PASS
- Attempts: 1
- Changes: frontend/src/components/workspace/MergeRequestBanner.tsx (new), frontend/src/pages/IdeaWorkspace/index.tsx, frontend/src/api/ideas.ts, frontend/src/hooks/use-websocket.ts, services/gateway/apps/ideas/views.py, 9 test files updated
- Tests: 718 Python tests passed, 403 Node tests passed (1 pre-existing failure in information-gaps-toggle.test.tsx). TypeScript typecheck clean.
- Learnings for future iterations:
  - merge_request_pending field added inline in _get_idea view (not in serializer) to avoid touching IdeaDetailSerializer which is used by create/patch too
  - MergeRequest model has `requested_by` field (not `requesting_idea_owner_id`) — use this for the requesting owner lookup
  - LockOverlay component already existed at components/workspace/LockOverlay.tsx — reused it with `relative` parent wrapper
  - Adding a new required field to the Idea interface requires updating ALL test mock objects — use `sed` carefully (multi-line arrays break simple line-based sed)
  - WebSocket handler dispatches `ws:merge_request` custom event; workspace listens and refetches idea to update merge_request_pending
  - Confirmation dialog for decline uses Dialog (Radix) component — matches component-specs.md § 17
  - Banner uses framer-motion AnimatePresence with slide-down (200ms) matching InvitationBanner pattern
  - `docker compose build` gate check has no docker-compose.yml (only test yml) — pre-existing issue
---
