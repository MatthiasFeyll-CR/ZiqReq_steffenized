# Ralph Progress Log — M7 (ai-chat)
Started: 2026-03-11T00:07:22+0100

---

## Implementation Log

<!-- Ralph appends per-story entries below as it works. -->
<!-- Each entry should include: story ID, what was done, files changed, -->
<!-- decisions made, and any deviations from the spec. -->

---

## Codebase Patterns

<!-- Ralph records reusable patterns discovered during implementation. -->
<!-- These are extracted by the PRD Writer for future milestones. -->

---

## Deviations & Decisions

<!-- Ralph records any spec deviations or implementation decisions -->
<!-- that the Spec Reconciler should review. -->

## US-001: AI Service Django Setup - Semantic Kernel Base
- Status: PASS
- Attempts: 1
- Changes: services/ai/agents/base.py, services/ai/ai_service/settings/base.py, services/ai/grpc_clients/core_client.py, services/ai/kernel/model_router.py, services/ai/kernel/sk_factory.py, services/ai/kernel/token_tracker.py
- Tests: 156 Python tests passed, 244 Node tests passed, ruff clean, mypy clean, tsc clean, Docker build OK
- Learnings for future iterations:
  - BaseAgent uses ABC with async process() as entry point, dispatches to _execute() or _load_mock_response() based on AI_MOCK_MODE
  - SK Kernel factory pattern: create_kernel(deployment_name, service_id) returns configured Kernel with AzureChatCompletion
  - Model router reads admin params via CoreClient.get_admin_parameter(), falls back to env vars
  - TokenTracker.record() extracts usage from SK ChatMessageContent metadata (usage.prompt_tokens / completion_tokens)
  - CoreClient.get_admin_parameter() stub added — returns empty value, future milestones wire to real gRPC
  - Host Python is 3.14, Docker uses 3.12 — can't install SK locally, but Docker build works
  - First pytest run may have transient errors (known issue), second run passes clean
  - Admin params already seeded in Core service migration 0002_seed_parameters.py
  - settings/base.py ADMIN_PARAM_DEFAULTS dict provides offline fallbacks for admin params
---

## US-002: Facilitator Agent Implementation
- Status: PASS
- Attempts: 1
- Changes: services/ai/agents/facilitator/agent.py, services/ai/agents/facilitator/prompt.py, services/ai/agents/facilitator/plugins.py, services/ai/events/publishers.py, services/ai/fixtures/facilitator_response.json, services/ai/tests/test_facilitator.py, .ralph/test-manifest.json, requirements-dev.txt
- Tests: 178 Python tests passed (22 new), 244 Node tests passed, ruff clean, tsc clean, Docker build OK
- Learnings for future iterations:
  - FacilitatorPlugin uses @kernel_function decorator for SK function registration — each tool is async
  - Event publisher uses in-memory list for M7 (events/publishers.py) — clear_published_events() in test fixtures
  - System prompt template uses Python str.format() not Handlebars — {{variable}} from spec becomes {variable}
  - Prompt is ~2800 tokens as specced; build_system_prompt() renders all conditional sections
  - Delegation tools are non-blocking: return delegation_id + "queued" immediately
  - request_board_changes stores instructions on plugin.board_instructions for pipeline to consume
  - Board ref validation: _validate_board_refs() strips [[Title]] brackets for invalid refs
  - Test container needed semantic-kernel + openai in requirements-dev.txt (not just AI service's requirements.txt)
  - BASE_DIR in tests points to gateway root, not AI service — mock mode tests need settings.BASE_DIR override
  - FacilitatorAgent._execute() uses service.get_chat_message_contents() with FunctionChoiceBehavior.Auto
  - SK config: max_auto_invoke_attempts=3, temperature=0.7, max_tokens=1000, frequency_penalty=0.3
---

## US-003: Chat Processing Pipeline Orchestrator
- Status: PASS
- Attempts: 1
- Changes: services/ai/processing/pipeline.py, services/ai/processing/context_assembler.py, services/ai/tests/test_pipeline.py, .ralph/test-manifest.json
- Tests: 191 Python tests passed (13 new), 244 Node tests passed, ruff clean, tsc clean, Docker build OK (backend services)
- Learnings for future iterations:
  - ChatProcessingPipeline uses class-level dicts (_versions, _abort_flags) for per-idea state tracking
  - Version tracking: _start_processing() increments version, _check_abort() validates before each step
  - Abort flag checked at step boundaries — PipelineAborted exception exits early without persisting
  - Delegation in M7 is a stub: logs "not available", re-invokes Facilitator with empty results
  - Board Agent in M7 is a stub: logs instructions but does not execute them
  - Step 6 publishes ai.processing.complete with counter_reset: true
  - Step 7 cleans up abort flag (version stays for mismatch detection)
  - ContextAssembler.assemble() maps GetIdeaContext gRPC response to FacilitatorAgent input_data format
  - FacilitatorAgent is imported lazily inside step methods to avoid circular imports
  - When patching lazily imported classes in tests, patch at the source module (agents.facilitator.agent.FacilitatorAgent), not the consuming module
  - Docker test container caches COPY layer — must rebuild (--no-cache or just rebuild) when source files change
  - .ralph/prd.json is a symlink to tasks/prd-m7.json — commit the real file path
---

## US-004: Debouncer Implementation
- Status: PASS
- Attempts: 1
- Changes: services/ai/processing/debouncer.py, services/ai/tests/test_debouncer.py, services/ai/ai_service/settings/base.py, .ralph/test-manifest.json
- Tests: 200 Python tests passed (9 new debouncer tests), 244 Node tests passed, ruff clean, tsc clean, Docker build OK
- Learnings for future iterations:
  - Debouncer uses asyncio.create_task + asyncio.sleep for per-idea timers
  - State machine: DebouncerState enum (IDLE, DEBOUNCING, PROCESSING) tracked per idea_id in dict
  - pipeline_callback is an async callable injected at construction — decouples debouncer from pipeline
  - reset() simply calls start() again which cancels existing timer and starts fresh
  - _get_debounce_timer() reads admin param via CoreClient, falls back to 3.0s default
  - Tests use short timers (0.05-0.15s) and asyncio.sleep for timing validation
  - cleanup() removes all state for an idea (use when idea is closed)
  - Added debounce_timer to ADMIN_PARAM_DEFAULTS in settings/base.py
  - mypy "duplicate module" error for events/__init__.py is pre-existing and expected (marked optional in gates)
---

## US-005: send_chat_message Tool
- Status: PASS
- Attempts: 1
- Changes: services/ai/tests/test_facilitator.py
- Tests: 205 Python tests passed (5 new send_chat_message tests), 244 Node tests passed, ruff clean, tsc clean
- Learnings for future iterations:
  - send_chat_message implementation was already complete from US-002; US-005 focused on comprehensive AC verification
  - Key tests added: full event payload verification (all 6 fields + event_id), created_at return, delegation message_type, board ref validation end-to-end through the tool, whitespace-only rejection, no-board-state edge case
  - Board ref validation strips [[Title]] brackets for refs not found in board_state.nodes — empty board_state means all refs stripped
  - publish_event auto-adds event_id (UUID) to every event payload
  - Docker build gate fails on unrelated "pdf" service (pre-existing apt issue) — not caused by AI service changes
  - .ralph/ is gitignored; prd.json symlinks to tasks/prd-m7.json — commit the real file
---

## US-006: react_to_message Tool
- Status: PASS
- Attempts: 1
- Changes: services/ai/tests/test_facilitator.py, .ralph/test-manifest.json
- Tests: 213 Python tests passed (8 new react_to_message tests), 244 Node tests passed, ruff clean, tsc clean
- Learnings for future iterations:
  - react_to_message implementation was already complete from US-002; US-006 focused on comprehensive AC verification
  - Key tests added: full event payload verification (all 4 fields + event_id), all 3 reaction types (thumbs_up, thumbs_down, heart), AI message rejection, duplicate rejection (UNIQUE), message not found, success return value, no events on error
  - Validation chain: reaction_type check → message lookup → sender_type check → has_ai_reaction check → publish
  - _find_message() searches recent_messages by str(msg.id) == message_id — message_id must be string
  - has_ai_reaction field on message objects is the duplicate detection mechanism (DB UNIQUE enforced at Gateway layer)
  - T-2.7.02 maps to test_react_duplicate_error — tests the UNIQUE constraint simulation via has_ai_reaction flag
---

## US-007: update_title Tool
- Status: PASS
- Attempts: 1
- Changes: services/ai/tests/test_facilitator.py, frontend/src/components/workspace/WorkspaceHeader.tsx, frontend/src/hooks/use-websocket.ts, frontend/src/pages/IdeaWorkspace/index.tsx, frontend/src/__tests__/title-update-animation.test.tsx, .ralph/test-manifest.json
- Tests: 221 Python tests passed (8 new update_title tests), 247 Node tests passed (3 new animation tests), ruff clean, tsc clean
- Learnings for future iterations:
  - update_title implementation was already complete from US-002; US-007 focused on comprehensive AC verification + frontend animation
  - Key backend tests: full event payload verification (idea_id, title, event_id), truncation at 60 chars (boundary test at exactly 60), title_manually_edited=true rejection, missing flag defaults to falsy (allows update), success return format
  - Frontend title animation: WorkspaceHeader uses framer-motion AnimatePresence + motion.span with key={idea.title} for cross-fade animation
  - Do NOT use AnimatePresence mode="wait" — jsdom doesn't complete framer-motion exit animations, so the old element blocks the new one in tests. Default mode (cross-fade) works correctly in tests
  - WebSocket title_update handling: use-websocket.ts dispatches a CustomEvent "ws:title_update" on window; IdeaWorkspacePage listens and updates idea state via refs to avoid stale closures
  - Docker test container caches COPY layers — must rebuild (docker compose up --build) when source files change, or tests run against stale code
  - T-2.3.03 frontend test verifies: motion span renders with data-testid="title-animated", title text updates on re-render with new idea prop
---
