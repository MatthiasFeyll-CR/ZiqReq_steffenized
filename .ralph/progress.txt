# Ralph Progress Log — M22 (chat-attachments)
Started: 2026-03-18T01:03:58+0100

---

## Implementation Log

<!-- Ralph appends per-story entries below as it works. -->
<!-- Each entry should include: story ID, what was done, files changed, -->
<!-- decisions made, and any deviations from the spec. -->

---

## Codebase Patterns

<!-- Ralph records reusable patterns discovered during implementation. -->
<!-- These are extracted by the PRD Writer for future milestones. -->

---

## Deviations & Decisions

<!-- Ralph records any spec deviations or implementation decisions -->
<!-- that the Spec Reconciler should review. -->

## US-001: MinIO service + storage abstraction layer
- Status: PASS
- Attempts: 1
- Changes: infra/docker/docker-compose.yml, requirements.txt, services/gateway/requirements.txt, services/gateway/gateway/settings/base.py, services/gateway/apps/projects/apps.py, services/gateway/storage/__init__.py, services/gateway/storage/backends.py, services/gateway/storage/factory.py, services/gateway/storage/tests/__init__.py, services/gateway/storage/tests/test_backends.py
- Tests: 21 new storage tests pass, 730 total Python tests pass, 362 frontend tests pass
- Learnings for future iterations:
  - Storage module lives at services/gateway/storage/ (not inside an app), importable as `storage.backends`, `storage.factory`
  - MinIO client is created as singleton via get_storage_backend() factory — call reset_storage_backend() in test tearDown
  - Bucket auto-creation runs in ProjectsConfig.ready() — guarded with try/except to not break startup if MinIO is unreachable
  - gateway_app AppConfig referenced in PRD notes does not exist — used ProjectsConfig.ready() instead
  - Pre-existing ruff errors in services/ (ai, core migrations) — not from our changes
  - Test Dockerfile uses root requirements.txt + requirements-dev.txt, so minio SDK needed in root requirements.txt too
  - MinIO healthcheck in docker-compose uses `mc ready local` command
  - presigned_get_object accepts extra_query_params dict for response-content-disposition
---

## US-002: Attachment model + migration (Gateway + Core)
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/projects/models.py, services/core/apps/projects/models.py, services/gateway/apps/projects/migrations/0007_attachment.py, services/gateway/apps/projects/tests/test_attachment_model.py
- Tests: 16 new model tests pass, 746 total Python tests pass, 362 frontend tests pass
- Learnings for future iterations:
  - App label is `gateway_projects` — migration dependencies reference this
  - Latest migration before this was 0006_project_favorite
  - Gateway Attachment is managed with db_table="attachments", Core mirror is managed=False with same db_table
  - Indexes: idx_attachments_project, idx_attachments_message, idx_attachments_project_deleted (composite for count queries)
  - active_count_for_project() is a classmethod that filters deleted_at__isnull=True
  - Pre-existing ruff errors still in services/ (ai, core migrations) — not from our changes
---

## US-003: Attachment REST API (upload, list, delete, presigned URL)
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/attachments/__init__.py, services/gateway/apps/attachments/apps.py, services/gateway/apps/attachments/serializers.py, services/gateway/apps/attachments/urls.py, services/gateway/apps/attachments/views.py, services/gateway/apps/attachments/tests/__init__.py, services/gateway/apps/attachments/tests/test_views.py, services/gateway/apps/projects/urls.py, services/gateway/gateway/settings/base.py
- Tests: 38 new endpoint tests pass, 784 total Python tests pass, 362 frontend tests pass
- Learnings for future iterations:
  - Attachments app registered as `apps.attachments` with label `gateway_attachments` in INSTALLED_APPS
  - URL patterns: attachments/ included under projects/<project_id>/attachments/ in apps/projects/urls.py
  - Auth pattern reused: _require_auth, _get_project_or_error, _check_access (same as chat views)
  - Storage backend accessed via _get_storage_backend() wrapper for easy mocking in tests
  - Rate limiting uses django.core.cache (defaults to LocMemCache in test, works with Redis in prod)
  - Cache key format: `upload_rate:{user_id}` with 60-second timeout
  - Filename sanitization: strip path, replace non-alphanumeric (except .-_) with underscore, collapse consecutive underscores, truncate to 255
  - DELETE is soft-delete (sets deleted_at), storage deletion is best-effort (logged on failure, doesn't block)
  - Presigned URL passes filename to storage backend for Content-Disposition: attachment header
  - MultiPartParser required for upload endpoint — use @parser_classes([MultiPartParser])
  - Test pattern: patch _get_storage_backend to return MagicMock() for all storage operations
---

## US-004: Chat-attachment linking + serializer/broadcast update
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/chat/serializers.py, services/gateway/apps/chat/views.py, services/gateway/apps/attachments/views.py, proto/core.proto, proto/core_pb2.py, proto/core_pb2.pyi, proto/core_pb2_grpc.py, services/ai/grpc_clients/core_client.py, services/gateway/apps/chat/tests/test_chat_attachments.py
- Tests: 12 new chat-attachment tests pass, 796 total Python tests pass, 362 frontend tests pass
- Learnings for future iterations:
  - ChatMessageCreateSerializer uses ListField(child=UUIDField(), max_length=3) for attachment_ids validation
  - _link_attachments helper validates: belongs to project, message_id is null, deleted_at is null — skips invalid with warning log
  - Attachment immutability: DELETE endpoint returns 400 if message_id is not null — prevents deletion of attachments already sent in a message
  - _list_messages does manual prefetch: queries all attachments for page's message IDs in one query, then maps them per-message
  - _broadcast_chat_message accepts optional attachments parameter and includes them in WebSocket payload
  - CoreServicer is dead (replaced by direct DB access) — AI core_client.py uses raw SQL. Updated _populate_message_attachments helper to fetch attachment metadata with SUBSTRING for preview
  - Proto stubs regenerated with: cd proto && python -m grpc_tools.protoc -I. --python_out=. --pyi_out=. --grpc_python_out=. core.proto
  - AttachmentMetadata message added to core.proto with extracted_content_preview (first 500 chars)
  - repeated AttachmentMetadata attachments added as field 8 to ChatMessage proto message
---

## US-005: File validation + security hardening
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/attachments/validators.py (new), services/gateway/apps/attachments/views.py, services/gateway/apps/attachments/tests/test_validators.py (new), services/gateway/apps/attachments/tests/test_views.py, services/gateway/requirements.txt, requirements.txt
- Tests: 33 new validator/security tests pass, 829 total Python tests pass, 362 frontend tests pass
- Learnings for future iterations:
  - validators.py contains: validate_magic_bytes, sanitize_image, sanitize_pdf, sanitize_filename, FileValidationError
  - Magic byte validation happens BEFORE upload to storage (prevents storing malicious files)
  - sanitize_image uses Pillow to open/clear info/re-save — strips EXIF metadata from images
  - sanitize_pdf uses PyMuPDF (fitz) to scan xref objects for /JS, /JavaScript, /Launch, /SubmitForm, /GoToR actions
  - fitz import is lazy (inside function) so missing PyMuPDF doesn't break startup — logs warning and returns data unchanged
  - sanitize_filename moved from views.py to validators.py for reuse
  - Presigned URLs already include response-content-disposition=attachment (from US-001 MinIOBackend)
  - Rate limiting already works via Redis cache (from US-003)
  - Pillow>=10.0 and PyMuPDF>=1.24 added to both gateway and root requirements.txt
  - Existing test helpers (_fake_pdf_bytes, _fake_png_bytes etc) needed updating to use real Pillow-generated images since sanitize_image now opens them with Pillow
  - Docker test containers need rebuild after requirements changes (stale image caused false test failures)
---

## US-006: AI content extraction — PDF processing
- Status: PASS
- Attempts: 1
- Changes: services/ai/ai_service/celery.py (new), services/ai/tasks/__init__.py (new), services/ai/tasks/extract_attachment.py (new), services/ai/tests/test_extract_attachment.py (new), services/ai/requirements.txt
- Tests: 15 new extraction tests pass, 844 total Python tests pass, 362 frontend tests pass
- Learnings for future iterations:
  - Celery app created at ai_service/celery.py — uses BROKER_URL env var (RabbitMQ) as broker
  - Task is NOT bind=True since retry logic isn't needed — simpler to test
  - _is_mock_mode() helper encapsulates django.setup() + settings check — patchable in tests
  - DB access pattern: direct SQL via django.db.connection (same as CoreClient pattern)
  - File download: direct MinIO client in _download_file() (AI service doesn't have gateway's storage abstraction)
  - Vision fallback: only triggered when page text < 50 chars AND page has images (expensive operation)
  - Truncation: _get_max_chars() reads attachment_extraction_max_tokens admin param, converts tokens to chars (×4)
  - Non-PDF content types are skipped (return without update) — US-007 will extend with image processing
  - Tests patch at grpc_clients.core_client.CoreClient (not tasks.extract_attachment.CoreClient) because import is lazy
  - Running specific test file paths in Docker fails due to conftest resolution — use -k filter or run full suite
  - celery, PyMuPDF, minio added to services/ai/requirements.txt (root requirements.txt already had them)
---

## US-007: AI content extraction — image processing + task orchestration
- Status: PASS
- Attempts: 1
- Changes: services/ai/tasks/extract_attachment.py, services/ai/processing/pipeline.py, services/gateway/apps/attachments/views.py, services/gateway/apps/websocket/consumers.py, services/gateway/events/consumers.py, services/ai/tests/test_extract_attachment.py, services/ai/tests/test_pipeline_extraction_wait.py (new)
- Tests: 25 new tests pass (19 extraction + 6 pipeline wait), 854 total Python tests pass, 362 frontend tests pass
- Learnings for future iterations:
  - Image extraction uses _extract_image() which delegates to _call_vision() with VISION_PROMPT_IMAGE
  - IMAGE_CONTENT_TYPES set: {"image/png", "image/jpeg", "image/webp"} — used for type routing in extraction task
  - Type check must happen BEFORE _get_max_chars/_download_file to avoid unnecessary MinIO calls for unsupported types
  - Celery task dispatch from Gateway uses Celery().send_task() pattern (not direct import) since AI service is separate
  - _dispatch_extraction_task reads CELERY_BROKER_URL from Django settings; skips if not configured
  - Pipeline extraction wait: _wait_for_extraction() polls get_project_context every 2s (max 30s), checks triggering message's attachments
  - Only the most recent user message (reversed iteration) is considered the "triggering message"
  - Event publishing: _publish_extraction_event_sync wraps async publish_event for use in Celery (sync) context
  - WebSocket: attachment_extracted handler added to ProjectConsumer; AIEventConsumer handles ai.attachment.extracted routing
  - Docker test containers MUST be rebuilt (--build) after code changes — stale images cause false test failures
  - Test container had stale code on first attempt, leading to confusing failures
---

## US-008: AI context integration (context assembler, prompt sandboxing, context extension)
- Status: PASS
- Attempts: 1
- Changes: services/ai/grpc_clients/core_client.py, services/ai/processing/context_assembler.py, services/ai/agents/facilitator/prompt.py, services/ai/agents/facilitator/agent.py, services/ai/agents/context_extension/agent.py, services/ai/agents/context_extension/prompt.py, services/ai/tests/test_context_assembly_attachments.py (new), services/ai/tests/test_context_extension.py (updated)
- Tests: 24 new context integration tests pass, 878 total Python tests pass, 362 frontend tests pass
- Learnings for future iterations:
  - CoreClient.get_project_attachments() queries attachments table directly (same pattern as other CoreClient methods)
  - ContextAssembler.assemble() now returns "attachments" key with list of completed-extraction attachments
  - _load_project_attachments() is a module-level helper that catches exceptions (returns [] on failure)
  - Facilitator prompt template uses {attachments_block} after </project> and {attachment_guidance_block} inside <conversation_rules>
  - _render_attachments_block() wraps each attachment in <user_attachment> with WARNING attribute for prompt injection sandboxing
  - Truncation per-attachment uses _get_max_content_chars() reading attachment_extraction_max_tokens admin param (default 16000 chars)
  - _format_messages() in both facilitator/agent.py and context_extension/prompt.py now appends "[Attachments: file1, file2]" to message content
  - Context extension agent now loads project attachments via get_project_attachments() and includes them in search prompt
  - build_system_prompt() in context_extension/prompt.py accepts optional attachments parameter (backward compatible)
  - Existing test_context_extension.py tests needed updates: mode string changed, empty history check also checks attachments
  - Docker test containers MUST be rebuilt after code changes (confirmed again — stale images cause false failures)
---

## US-009: Frontend: attachment API, upload hook, ChatInput + drag & drop
- Status: PASS
- Attempts: 1
- Changes: frontend/src/api/attachments.ts (new), frontend/src/hooks/use-attachment-upload.ts (new), frontend/src/components/chat/AttachmentStagingArea.tsx (new), frontend/src/components/chat/ChatDropZone.tsx (new), frontend/src/components/chat/ChatInput.tsx, frontend/src/api/chat.ts, frontend/src/i18n/locales/en.json, frontend/src/i18n/locales/de.json, frontend/src/__tests__/use-attachment-upload.test.tsx (new), frontend/src/__tests__/chat-input-attachments.test.tsx (new), tasks/prd-m22.json
- Tests: 14 new frontend tests pass, 376 total frontend tests pass, TypeScript typecheck clean
- Learnings for future iterations:
  - Upload API uses XMLHttpRequest (not fetch) for progress events — uploadAttachment returns Promise wrapping XHR
  - getAccessToken() from auth-token.ts provides Bearer token for XHR (authFetch only works with fetch API)
  - useAttachmentUpload hook manages pending uploads with unique IDs (incrementing counter via useRef)
  - Validation matches backend: 100MB max, 3 per message, 10 per project, types: png/jpg/jpeg/webp/pdf
  - AttachmentStagingArea uses URL.createObjectURL for image thumbnails — must revoke on unmount to prevent memory leaks
  - ChatDropZone uses dragCounter state to handle nested drag enter/leave events correctly
  - canSend logic: true if (text non-empty OR staged attachments exist) AND not sending AND not disabled AND not uploading
  - handleSend passes " " (single space) as content when text is empty but attachments exist — backend requires non-empty content
  - ChatInput now accepts optional projectAttachmentCount prop to enforce project-level limits
  - File input reset (e.target.value = "") after onChange allows re-selecting the same file
  - react-toastify toast.error() used for user-facing validation errors (same pattern as useRateLimit)
  - QuickReplyChips hidden when staged attachments exist (same as when text is non-empty)
  - Pre-existing ruff errors in services/ (ai, core migrations) remain — not from US-009 changes
---

## US-010: Frontend: message attachment display, thumbnails, presigned URL access
- Status: PASS
- Attempts: 1
- Changes: frontend/src/components/chat/AttachmentBox.tsx (new), frontend/src/components/chat/UserMessageBubble.tsx, frontend/src/components/chat/ChatMessageList.tsx, frontend/src/components/workspace/ChatPanel.tsx, frontend/src/hooks/use-websocket.ts, frontend/src/i18n/locales/en.json, frontend/src/i18n/locales/de.json, frontend/src/__tests__/attachment-box.test.tsx (new), frontend/src/__tests__/user-message-attachments.test.tsx (new), .ralph/prd.json
- Tests: 15 new frontend tests pass (10 AttachmentBox + 5 UserMessageBubble), 391 total frontend tests pass, TypeScript typecheck clean
- Learnings for future iterations:
  - AttachmentBox component is reusable: accepts clickable, onRemove, showThumbnail, thumbnailUrl props
  - isReadOnly prop flows: ProjectWorkspace → ChatPanel → ChatMessageList → UserMessageBubble → AttachmentBox (clickable=!isReadOnly)
  - WebSocket event for extraction: "attachment_extracted" dispatched as CustomEvent "ws:attachment_extracted" with {project_id, attachment_id, extraction_status}
  - ChatPanel handles ws:attachment_extracted by mapping over newMessages and updating matching attachment's extraction_status
  - Presigned URLs are fetched fresh on each click (15-min TTL, never cached)
  - Read-only users see attachment boxes with cursor-not-allowed; clicking shows toast via react-toastify
  - Extraction status display: pending/processing shows pulse animation + "Processing...", failed shows muted "Extraction failed", completed shows nothing extra
  - Pre-existing ruff errors in services/ (ai, core migrations) remain — not from US-010 changes
---
