# Ralph Progress Log — M1 (foundation)
Started: 2026-03-09T17:27:54+0100

---

## Implementation Log

<!-- Ralph appends per-story entries below as it works. -->
<!-- Each entry should include: story ID, what was done, files changed, -->
<!-- decisions made, and any deviations from the spec. -->

---

## Codebase Patterns

<!-- Ralph records reusable patterns discovered during implementation. -->
<!-- These are extracted by the PRD Writer for future milestones. -->

---

## Deviations & Decisions

<!-- Ralph records any spec deviations or implementation decisions -->
<!-- that the Spec Reconciler should review. -->

## US-001: Drag to move nodes with position persistence
- Status: PASS
- Attempts: 1
- Changes: frontend/src/api/board.ts (new), frontend/src/components/board/BoardCanvas.tsx, frontend/src/components/workspace/WorkspaceLayout.tsx, frontend/src/pages/IdeaWorkspace/index.tsx, frontend/src/__tests__/board-interactions.test.tsx (new), .ralph/test-manifest.json
- Tests: 142 passed (19 test files), including 3 new T-3.2.01 tests
- Learnings for future iterations:
  - API pattern uses fetch (not axios) with `env.apiBaseUrl` from `@/config/env` — match ideas.ts pattern
  - @xyflow/react does not export `NodeDragHandler` type; use inline `(_event: React.MouseEvent, node: Node) => void`
  - BoardCanvas had no props — added `ideaId?: string` prop, threaded through WorkspaceLayout from IdeaWorkspacePage
  - `onNodeDragStop` fires only on drag end (not during), correct for PATCH-on-end requirement
  - Docker build gate (`docker compose build`) fails on pre-existing nginx.conf issue — not related to source changes
  - vi.fn() needs explicit generic for typed mock params: `vi.fn<(a: string, b: string, c: Record<string, unknown>) => Promise<unknown>>()`
---

## US-002: Group nesting (drag into/out of groups)
- Status: PASS
- Attempts: 1
- Changes: frontend/src/components/board/BoardCanvas.tsx, frontend/src/components/board/GroupNode.tsx, frontend/src/__tests__/board-interactions.test.tsx, .ralph/test-manifest.json, .ralph/prd.json
- Tests: 149 passed (19 test files), including 7 new tests (T-3.2.02, T-3.2.03, visual feedback, nested groups)
- Learnings for future iterations:
  - @xyflow/react v12 Node type does NOT have `positionAbsolute` — it's on `InternalNode.internals.positionAbsolute`. Use `useReactFlow().getInternalNode(id)` to access absolute positions.
  - `findParentGroup` selects the smallest-area group containing the absolute position, which naturally handles nested groups.
  - React Flow handles child-moves-with-parent natively when `parentId` is set on a node.
  - When reparenting, recalculate position relative to new parent (absX - parentAbsX).
  - Set `expandParent: true` on child nodes so the parent group auto-expands if the child is near the edge.
  - Visual drop zone feedback uses `_dropTarget` flag in group node data, toggled during `onNodeDrag`.
  - Docker test container copies files at build time — must `docker compose -f docker-compose.test.yml down && up -d --build` after changing test files.
  - Mock `useNodesState` must be stateful (using a module-level variable + callback-aware mockSetNodes) for tests that verify state mutations via setNodes callbacks.
---

## US-003: Node lock toggle
- Status: PASS
- Attempts: 1
- Changes: frontend/src/components/board/BoxNode.tsx, frontend/src/components/board/GroupNode.tsx, frontend/src/components/board/FreeTextNode.tsx, frontend/src/components/board/BoardCanvas.tsx, frontend/src/__tests__/board-interactions.test.tsx, frontend/src/__tests__/box-node.test.tsx, .ralph/test-manifest.json, .ralph/prd.json
- Tests: 159 passed (19 test files), including 10 new tests (T-3.2.07 lock toggle tests across BoardCanvas and BoxNode)
- Learnings for future iterations:
  - Lock toggle pattern: Each node component accepts `onToggleLock?: (nodeId: string, newLocked: boolean) => void` in its data interface. BoardCanvas injects this callback via `processedNodes` useMemo that maps over nodes before passing to ReactFlow.
  - `processedNodes` pattern: Use `useMemo` to derive nodes with injected callbacks and computed props (`draggable: !is_locked`) before passing to ReactFlow. This keeps node components pure and BoardCanvas as the single source of truth for lock state management.
  - Lock + delete protection: `handleDeleteSelected` filters out nodes with `data.is_locked` to prevent locked nodes from being deleted.
  - FreeTextNode edit prevention: When `is_locked` is true, `handleClick` returns early to prevent entering edit mode.
  - GroupNode resize prevention: `NodeResizer` has `isVisible` prop — set to `!is_locked` to hide resize handles on locked groups.
  - ReactFlow mock needs `screenToFlowPosition` for tests that render BoardToolbar (used in handleAddBox).
  - Capture `nodes` prop from ReactFlow mock to verify `draggable` and injected data props in tests.
  - `.ralph` directory is gitignored — use `git add -f` for files under it.
---

## US-004: Multi-select with selection box
- Status: PASS
- Attempts: 1
- Changes: frontend/src/components/board/BoardCanvas.tsx, frontend/src/__tests__/board-canvas.test.tsx, frontend/src/__tests__/board-interactions.test.tsx, .ralph/prd.json
- Tests: 159 passed (19 test files), all pre-existing tests still green
- Learnings for future iterations:
  - React Flow multi-select: `selectionOnDrag` + `selectionKeyCode="Control"` + `selectionMode={SelectionMode.Partial}` enables Ctrl+drag selection box
  - `SelectionMode` must be imported from `@xyflow/react` and added to test mocks (like `BackgroundVariant` and `MarkerType`)
  - `deleteKeyCode={null}` disables React Flow's built-in delete to use custom `handleDeleteSelected` that respects locked nodes
  - Added keyboard listener for Delete/Backspace keys that calls `handleDeleteSelected`, with input/textarea guard to prevent deleting while editing text
---

## US-005: Undo/redo Redux system
- Status: PASS
- Attempts: 1
- Changes: frontend/src/store/board-slice.ts, frontend/src/hooks/use-board-undo.ts, frontend/src/components/board/BoardCanvas.tsx, frontend/src/components/board/BoardToolbar.tsx, frontend/src/__tests__/board-undo.test.ts (new), frontend/src/__tests__/board-undo-integration.test.ts (new), frontend/src/__tests__/board-interactions.test.tsx, frontend/src/__tests__/board-canvas.test.tsx, .ralph/test-manifest.json, .ralph/prd.json
- Tests: 169 passed (21 test files), including 10 new undo/redo tests (T-3.7.01, T-3.7.02, T-3.7.04, T-3.7.05)
- Learnings for future iterations:
  - board-slice uses createSlice with typed `UndoEntry` interface: `{type, nodeId, data, previousState, source}`. Selectors exported for undoTop/redoTop/canUndo/canRedo.
  - `useBoardUndo` hook wraps dispatch + selectors. On undo reads `undoTop.previousState` before dispatching, sends PATCH with that data. On redo reads `redoTop.data`.
  - BoardCanvas now imports useBoardUndo and pushes undo entries on drag-end and lock-toggle. The hook needs react-redux Provider.
  - All test files rendering BoardCanvas must mock react-redux: `vi.mock("react-redux", () => ({ useDispatch: () => vi.fn(), useSelector: (selector) => selector({ board: { undoStack: [], redoStack: [], selectedNodeIds: [] } }) }))`.
  - TS lib doesn't include es2022 — `Array.at()` unavailable, use bracket access instead.
  - BoardToolbar interface extended with optional undo/redo props (onUndo, onRedo, canUndo, canRedo, undoTopSource, redoTopSource) for US-006 to use.
  - Pure Redux reducer tests (board-undo.test.ts) don't need any component mocking — test the reducer function directly with `boardReducer(state, action)`.
---

## US-006: Undo/redo buttons with context-aware labels
- Status: PASS
- Attempts: 1
- Changes: frontend/src/components/board/BoardToolbar.tsx, frontend/src/__tests__/board-toolbar-undo.test.tsx (new), .ralph/test-manifest.json, .ralph/prd.json
- Tests: 177 passed (22 test files), including 8 new toolbar undo/redo tests (T-3.7.03)
- Learnings for future iterations:
  - BoardToolbar already had undo/redo props in interface from US-005 — just needed to destructure and wire up the buttons.
  - Context-aware label pattern: `title={source === "ai" ? "Undo AI Action" : "Undo"}` — simple ternary on stack top source.
  - Toolbar tests can mock `@xyflow/react` minimally (just `useReactFlow` returning `fitView` and `screenToFlowPosition`) since toolbar doesn't use ReactFlow directly.
  - `disabled={!canUndo}` with default `canUndo = false` handles the empty-stack case cleanly.
---

## US-007: Board item reference insertion to chat
- Status: PASS
- Attempts: 1
- Changes: frontend/src/components/chat/ChatInput.tsx, frontend/src/__tests__/chat-board-reference.test.tsx (new), .ralph/test-manifest.json, .ralph/prd.json
- Tests: 181 passed (23 test files), including 4 new chat board reference tests (T-3.8.01, T-3.8.02)
- Learnings for future iterations:
  - BoxNode already dispatches `CustomEvent('board:reference', { detail: '@board[nodeId]' })` from M4 — no changes needed on the board side.
  - ChatInput has `textareaRef` for cursor manipulation. Use `selectionStart`/`selectionEnd` to insert at cursor and `requestAnimationFrame` + `setSelectionRange` to reposition cursor after React state update.
  - `setValue` with functional update `(prev) => ...` ensures correct insertion even with stale closures in the event listener (empty deps array `[]`).
  - Test pattern: use `act(() => { window.dispatchEvent(...) })` to trigger custom events in tests, then assert textarea value directly.
  - T-3.8.01 tests already existed in box-node.test.tsx from M4 — just registered in test manifest.
---