# Ralph Progress Log — M6 (websocket)
Started: 2026-03-10T03:55:44+0100

---

## Implementation Log

<!-- Ralph appends per-story entries below as it works. -->
<!-- Each entry should include: story ID, what was done, files changed, -->
<!-- decisions made, and any deviations from the spec. -->

---

## Codebase Patterns

<!-- Ralph records reusable patterns discovered during implementation. -->
<!-- These are extracted by the PRD Writer for future milestones. -->

---

## Deviations & Decisions

<!-- Ralph records any spec deviations or implementation decisions -->
<!-- that the Spec Reconciler should review. -->

## US-001: Django Channels WebSocket consumer — connect, token auth, disconnect, error handling
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/websocket/consumers.py, services/gateway/apps/websocket/middleware.py, services/gateway/apps/websocket/tests/test_consumers.py, services/gateway/gateway/asgi.py, services/gateway/gateway/settings/test.py, .ralph/test-manifest.json
- Tests: 6 passed (test_connection_valid_token, test_connection_invalid_token, test_connection_missing_token, test_connection_nonexistent_user_token, test_error_on_unknown_message_type, test_error_on_missing_message_type). Full suite: 116 Python tests passed, 191 Node tests passed.
- Learnings for future iterations:
  - WebSocketAuthMiddleware is an ASGI middleware wrapping URLRouter in asgi.py. It validates token from query string and populates scope["user_id"] and scope["user_display_name"]
  - In dev bypass mode (AUTH_BYPASS=True), the token is treated as a user UUID directly (no Azure AD validation)
  - IdeaConsumer inherits AsyncJsonWebsocketConsumer, stores user_id, user_display_name, connection_id (uuid4), and subscribed_groups (set) on connect
  - Consumer rejects connection (close code 4003) if scope["user_id"] is missing (middleware didn't set it)
  - receive_json dispatches to handle_{type} methods, returns error JSON for unknown/missing types
  - Tests use channels.testing.WebsocketCommunicator with the full middleware+router stack
  - Docker test containers copy code at build time — must rebuild python-tests after code changes
  - InMemoryChannelLayer added to test settings for channel layer operations
  - WebsocketCommunicator does NOT expose .instance attribute — can't directly inspect consumer state in tests
  - The docker compose build gate check has a pre-existing failure (frontend Dockerfile references nginx.conf that doesn't exist at that path)
---

## US-002: Channel group management — subscribe_idea, unsubscribe_idea, group join/leave
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/websocket/consumers.py, services/gateway/apps/websocket/tests/test_consumers.py, .ralph/test-manifest.json
- Tests: 16 consumer tests passed (6 from US-001 + 10 new). Full suite: 126 Python tests passed, 191 Node tests passed.
- Learnings for future iterations:
  - handle_subscribe_idea validates UUID format, checks access (owner/co_owner/collaborator via Idea + IdeaCollaborator models), then does group_add
  - handle_unsubscribe_idea is a simple group_discard + remove from subscribed_groups set
  - Channel group naming convention: "idea_{uuid}"
  - Access check uses database_sync_to_async wrapper to query gateway's Idea and IdeaCollaborator models
  - Successful subscribe/unsubscribe are silent (no response sent) — only errors send a response
  - Duplicate subscribe is a no-op (checks subscribed_groups set first)
  - Unsubscribe for non-subscribed group is also a no-op
  - Running tests on individual file fails with content_type UniqueViolation — must run via full pytest (conftest handles persistent test DB)
---

## US-003: Chat message broadcast — on REST POST success, broadcast chat.message.created to idea group
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/websocket/consumers.py, services/gateway/apps/chat/views.py, services/gateway/apps/websocket/tests/test_consumers.py, .ralph/test-manifest.json
- Tests: 3 new tests passed (test_chat_message_broadcast_to_subscriber, test_chat_message_broadcast_not_received_by_unsubscribed, test_chat_message_broadcast_via_view_helper). Full suite: 128 Python tests passed, 191 Node tests passed. Ruff clean. TSC clean.
- Learnings for future iterations:
  - Broadcasting from a sync Django REST view uses `async_to_sync(channel_layer.group_send)()` pattern (from asgiref)
  - `get_channel_layer()` returns the configured channel layer (InMemoryChannelLayer in tests, Redis in prod)
  - Consumer group message handlers (e.g. `chat_message`) are called by channel layer's group_send — the `type` field maps to method name (dots become underscores)
  - The `_broadcast_chat_message` helper is a fire-and-forget with exception logging — broadcast failure doesn't break the REST response
  - Payload includes: id, sender_type, sender{id, display_name}, ai_agent, content, message_type, created_at — matching the WebSocket protocol spec
  - No RabbitMQ event publishing exists yet in the codebase — the "broker event" from the PRD is implemented directly via channel_layer.group_send instead
---

## US-004: Board sync — broadcast board.node.updated / board.connection.updated after REST persistence
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/websocket/consumers.py, services/gateway/apps/board/views.py, services/gateway/apps/websocket/tests/test_consumers.py, .ralph/test-manifest.json
- Tests: 5 new tests passed (test_board_update_broadcast_to_subscriber, test_board_update_not_received_by_unsubscribed, test_board_update_broadcast_via_view_helper, test_board_update_broadcast_node_delete, test_board_update_broadcast_source_ai). Full suite: 134 Python tests passed, 191 Node tests passed. Ruff clean. TSC clean.
- Learnings for future iterations:
  - `_broadcast_board_update` follows same pattern as `_broadcast_chat_message` — fire-and-forget with exception logging via `async_to_sync(channel_layer.group_send)()`
  - Board broadcast payload batches changes: nodes_created, nodes_updated, nodes_deleted, connections_created, connections_updated, connections_deleted, source
  - `source` field is "user" or "ai" — derived from `created_by` on node creation, hardcoded "user" on REST update/delete (only users trigger REST calls)
  - Helper functions `_node_to_broadcast_dict` and `_connection_to_broadcast_dict` serialize model instances for broadcast payload
  - Consumer `board_update` handler is a simple passthrough like `chat_message` — forwards group_send to WebSocket client
  - All 6 board CRUD endpoints (create/update/delete for both nodes and connections) now broadcast after persistence
---

## US-005: Board awareness events — selection/deselection, lock state changes (WebSocket-only, no persistence)
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/websocket/consumers.py, services/gateway/apps/board/views.py, services/gateway/apps/websocket/tests/test_consumers.py, .ralph/test-manifest.json
- Tests: 4 new tests passed (test_board_selection_broadcast_excludes_sender, test_board_selection_deselect_null_node, test_board_selection_not_subscribed_error, test_board_lock_change_broadcast). Full suite: 138 Python tests passed, 191 Node tests passed. Ruff clean. TSC clean.
- Learnings for future iterations:
  - `handle_board_selection` validates the user is subscribed to the idea group before broadcasting — returns error if not subscribed
  - Board selection is ephemeral (no persistence) — consumer receives client message and immediately broadcasts to group via `group_send`
  - Sender exclusion: `sender_channel` is included in the group_send event dict; the `board_selection` handler compares it to `self.channel_name` and skips forwarding if they match
  - `board_lock_change` is broadcast from board views `_update_node` when `is_locked` field changes — detected by comparing `data["is_locked"]` against `node.is_locked` before applying update
  - `_broadcast_board_lock_change` follows same fire-and-forget pattern as `_broadcast_board_update` and `_broadcast_chat_message`
  - The user info in selection payload is populated server-side from `self.user_id` / `self.user_display_name` (not trusted from client)
---

## US-006: Frontend WebSocket connection manager — connect on auth, reconnect with exponential backoff, session-level
- Status: PASS
- Attempts: 1
- Changes: frontend/src/store/websocket-slice.ts, frontend/src/hooks/use-websocket.ts (new), frontend/src/app/providers.tsx, frontend/src/__tests__/websocket-slice.test.ts (new), frontend/src/__tests__/use-websocket.test.ts (new), .ralph/test-manifest.json
- Tests: 16 new tests passed (8 websocket-slice, 8 use-websocket hook). Full suite: 138 Python tests passed, 207 Node tests passed. TSC clean. Ruff clean.
- Learnings for future iterations:
  - `websocket-slice.ts` stores `connectionState` ('online'|'offline') and `reconnectCountdown` (seconds until next retry or null)
  - `useWebSocket` hook manages the WebSocket lifecycle: connects when `isAuthenticated && user`, disconnects on logout/unmount
  - Token for WebSocket is `user.id` (in dev bypass mode, backend treats token as user UUID directly)
  - `WebSocketManager` component in `providers.tsx` calls `useWebSocket()` — mounted inside AuthProvider so auth context is available
  - Exponential backoff: 1s, 2s, 4s, 8s, 16s, 30s (capped) — backoff resets to 1s on successful open
  - `intentionalCloseRef` prevents reconnection when user explicitly disconnects (logout) vs unexpected close
  - Cleanup on unmount: clears both reconnect timeout and countdown interval timers (LOOP-004 safety)
  - `env.wsBaseUrl` from `VITE_WS_BASE_URL` (default `/ws`) — hook builds full ws:// URL from window.location for relative paths
  - react-redux v9 requires `children` as a prop in Provider, not as third createElement argument
  - MockWebSocket class in tests uses `vi.stubGlobal("WebSocket", MockWebSocket)` for clean test isolation
  - To test escalating backoff, simulate close without open (open resets backoff to initial value)
---

## US-007: Presence tracking — online/idle/offline states, stacked avatars in workspace header, multi-tab dedup
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/websocket/consumers.py, frontend/src/store/presence-slice.ts, frontend/src/hooks/use-websocket.ts, frontend/src/app/providers.tsx, frontend/src/components/workspace/PresenceIndicators.tsx (new), frontend/src/components/workspace/WorkspaceHeader.tsx, frontend/src/__tests__/presence-slice.test.ts (new), frontend/src/__tests__/presence-indicators.test.tsx (new), services/gateway/apps/websocket/tests/test_consumers.py, .ralph/test-manifest.json, frontend/src/__tests__/workspace-header.test.tsx, frontend/src/__tests__/section-visibility.test.tsx
- Tests: 4 new backend tests passed (test_presence_broadcast_on_subscribe, test_presence_multi_tab_dedup, test_presence_offline_on_unsubscribe, test_presence_update_client_message). 11 new frontend tests passed (7 presence-slice, 4 presence-indicators). Full suite: 142 Python tests passed, 218 Node tests passed. TSC clean. Ruff clean.
- Learnings for future iterations:
  - Server-side presence uses a module-level `_presence_registry` dict: `group_name -> {user_id -> set(channel_names)}` for multi-tab dedup
  - When a user subscribes from tab 1: online broadcast. Tab 2: no broadcast (already present). Tab 1 disconnects: no offline. Tab 2 disconnects: offline broadcast.
  - `_add_presence` and `_remove_presence` are async helpers on IdeaConsumer that manage the registry and broadcast
  - Subscribe now broadcasts presence_update, so ALL existing tests that used `receive_nothing` after subscribe needed updating to drain the presence message
  - Created `_subscribe_and_drain()` test helper that subscribes and consumes the presence broadcast
  - For multi-user tests (2 communicators), after both subscribe, the first user gets a cross-presence message from the second user that also needs draining
  - `useWebSocket` hook now has `ws.onmessage` handler that dispatches `updatePresence` to Redux for `presence_update` messages
  - `useWsSend` context/hook exported from providers.tsx gives components access to WebSocket send capability
  - PresenceIndicators uses Avatar component with userId-based hue coloring, status badges (green/yellow/gray), and tooltip
  - Adding PresenceIndicators to WorkspaceHeader required updating existing workspace-header and section-visibility tests to wrap renders in Redux Provider
  - The `_presence_registry` is cleared between tests via `autouse=True` pytest fixture
---

## US-008: User selection highlights on board — colored borders with username when another user selects a node
- Status: PASS
- Attempts: 1
- Changes: frontend/src/store/selections-slice.ts (new), frontend/src/store/index.ts, frontend/src/hooks/use-websocket.ts, frontend/src/components/board/BoardCanvas.tsx, frontend/src/components/board/BoxNode.tsx, frontend/src/components/board/GroupNode.tsx, frontend/src/components/board/FreeTextNode.tsx, frontend/src/components/board/UserSelectionHighlight.tsx (new), frontend/src/__tests__/selections-slice.test.ts (new), frontend/src/__tests__/user-selection-highlight.test.tsx (new), frontend/src/__tests__/board-interactions.test.tsx, frontend/src/__tests__/board-canvas.test.tsx, frontend/src/__tests__/ai-modified-indicator.test.tsx, .ralph/test-manifest.json
- Tests: 16 new tests passed (13 selections-slice, 3 user-selection-highlight). Full suite: 142 Python tests passed, 234 Node tests passed. TSC clean. Ruff clean.
- Learnings for future iterations:
  - `selections-slice.ts` tracks `byIdea: Record<string, UserSelection[]>` where UserSelection has user_id, display_name, node_id
  - `getUserColor(userId)` provides deterministic color assignment using a hash of the user ID modulo 8 predefined colors
  - `UserSelectionHighlight` component wraps node content with a colored box-shadow border + name label positioned above the node
  - BoxNode and FreeTextNode use the wrapper component; GroupNode handles selection inline (since it already has dynamic border styling for drop targets)
  - BoardCanvas reads selections from Redux via `useSelector`, filters out own user's selections, and passes `_selectedBy` through node data
  - `onNodeClick` sends `board_selection` with the node_id; `onPaneClick` sends deselection (node_id=null) using a `selectedNodeRef` to track current selection
  - `useWebSocket` hook dispatches `updateSelection` for incoming `board_selection` messages (alongside existing `updatePresence` for `presence_update`)
  - Adding `useSelector`, `useWsSend`, `useAuth` to BoardCanvas broke existing tests (board-interactions, board-canvas, ai-modified-indicator) — needed to add `selections: { byIdea: {} }` to mock Redux state and mock `useWsSend`/`useAuth`
  - The `ai-modified-indicator` test uses `vi.doMock` + `vi.resetModules` pattern — mock additions must use `vi.doMock` (not `vi.mock`) to match
---

## US-009: Offline banner + behavior — 'Currently offline' with countdown, chat/board locked, reconnect button
- Status: PASS
- Attempts: 1
- Changes: frontend/src/components/common/OfflineBanner.tsx (new), frontend/src/hooks/use-websocket.ts, frontend/src/app/providers.tsx, frontend/src/pages/IdeaWorkspace/index.tsx, frontend/src/components/workspace/WorkspaceLayout.tsx, frontend/src/components/board/BoardCanvas.tsx, frontend/src/__tests__/offline-banner.test.tsx (new), frontend/src/__tests__/idea-workspace.test.tsx, frontend/src/__tests__/section-visibility.test.tsx, .ralph/test-manifest.json
- Tests: 6 new tests passed (renders banner when offline, hides when online, shows countdown, shows generic reconnecting text, calls reconnectNow on click, reconnect button present). Full suite: 142 Python tests passed, 240 Node tests passed. TSC clean. Ruff clean.
- Learnings for future iterations:
  - `OfflineBanner` uses framer-motion `AnimatePresence` + `motion.div` for slide-down animation on mount/unmount
  - `reconnectNow` function added to `useWebSocket` hook — clears timers, resets backoff, and calls `connect()` immediately
  - `useWsReconnect` context/hook exported from providers.tsx alongside `useWsSend` — gives components access to manual reconnect
  - IdeaWorkspaceContent now uses `useSelector(selectIsOnline)` to derive `effectiveChatLocked` and `effectiveLockReason` when offline
  - `WorkspaceLayout` accepts `disabled` prop, passes it to `BoardCanvas`
  - `BoardCanvas` disabled mode: sets `nodesDraggable=false`, `nodesConnectable=false`, `elementsSelectable=false`, and nullifies all interaction callbacks (onNodesChange, onNodeClick, onNodeDrag, etc.)
  - Adding `useSelector` to IdeaWorkspaceContent broke `idea-workspace.test.tsx` and `section-visibility.test.tsx` — both needed Redux Provider wrapping with `websocketReducer` and `useWsReconnect` mock
  - Tests that render IdeaWorkspacePage must preload websocket state as "online" (default is "offline") to preserve existing section-visibility behavior
---

## US-010: Connection state indicator — navbar green/red dot + Online/Offline label
- Status: PASS
- Attempts: 1
- Changes: frontend/src/components/layout/ConnectionIndicator.tsx, frontend/src/__tests__/connection-indicator.test.tsx (new), frontend/src/__tests__/dev-login-flow.test.tsx, frontend/src/__tests__/landing-page.test.tsx, frontend/src/i18n/locales/en.json, frontend/src/i18n/locales/de.json, .ralph/test-manifest.json
- Tests: 4 new tests passed (shows green dot Online, shows red dot Offline, transition-colors duration-200, hidden sm:inline). Full suite: 142 Python tests passed, 234 Node tests passed. TSC clean. Ruff clean.
- Learnings for future iterations:
  - ConnectionIndicator reads `selectConnectionState` from websocket-slice Redux store — green dot + "Online" or red dot + "Offline"
  - Added `common.offline` translation key to en.json and de.json (was missing, only `common.online` existed)
  - `transition-colors duration-200` on the dot provides the 200ms crossfade when state changes
  - Label uses `hidden sm:inline` so only the dot shows on small screens
  - Adding `useSelector` to ConnectionIndicator broke tests that render Navbar without Redux Provider — `dev-login-flow.test.tsx` and `landing-page.test.tsx` both needed wrapping with `Provider` + `configureStore` with websocketReducer
  - Pattern: any component using `useSelector` that's rendered inside Navbar will break tests that render pages with Navbar but without Redux Provider
---
