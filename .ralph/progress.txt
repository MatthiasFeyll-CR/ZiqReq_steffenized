# Ralph Progress Log — M11 (collaboration)
Started: 2026-03-11T07:34:47+0100

---

## Implementation Log

<!-- Ralph appends per-story entries below as it works. -->
<!-- Each entry should include: story ID, what was done, files changed, -->
<!-- decisions made, and any deviations from the spec. -->

---

## Codebase Patterns

<!-- Ralph records reusable patterns discovered during implementation. -->
<!-- These are extracted by the PRD Writer for future milestones. -->

---

## Deviations & Decisions

<!-- Ralph records any spec deviations or implementation decisions -->
<!-- that the Spec Reconciler should review. -->

## US-001: Invitation REST API
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/collaboration/views.py, services/gateway/apps/collaboration/urls.py, services/gateway/apps/ideas/urls.py, services/gateway/apps/collaboration/tests/test_invitation_api.py
- Tests: 581 Python tests passed, 361 Node tests passed (45 files). 1 pre-existing node error (PDFPreview unrelated).
- Learnings for future iterations:
  - send_invitation is mounted under ideas URLs at `<idea_id>/collaborators/invite` while accept/decline/revoke are under collaboration URLs at `api/invitations/<id>/<action>`
  - IdeaCollaborator uses ForeignKey to Idea (not raw UUID) — use `idea=idea` or `idea_id=idea.id` for create
  - `_require_auth` and `_unauthorized_response` helper pattern used in ideas views — replicated in collaboration views
  - Auth bypass with `override_settings(DEBUG=True, AUTH_BYPASS=True)` and dev-login for tests
  - Docker build gate check has pre-existing failure (missing nginx.conf) — not caused by M11 changes
  - Visibility transition uses `Idea.objects.filter(..., visibility="private").update(visibility="collaborating")` — only updates if still private, safe for idempotency
  - `transaction.atomic()` wraps accept flow: invitation status + collaborator create + visibility transition
---

## US-002: Collaborator Management API
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/collaboration/views.py, services/gateway/apps/ideas/urls.py, services/gateway/apps/collaboration/tests/test_collaborator_mgmt.py, .ralph/test-manifest.json
- Tests: 595 Python tests passed (14 new), 361 Node tests passed. 1 pre-existing node error (PDFPreview).
- Learnings for future iterations:
  - All 4 new endpoints (list, remove, transfer, leave) mounted under ideas URLs at `<idea_id>/collaborators`, `<idea_id>/collaborators/<user_id_param>`, `<idea_id>/transfer-ownership`, `<idea_id>/leave`
  - Transfer ownership uses `transaction.atomic()`: remove new owner from collaborators, add previous owner as collaborator, update `owner_id`
  - Co-owner leave sets `co_owner_id=NULL` directly (not via collaborators table). Co-owners are not in `idea_collaborators`.
  - Single owner constraint: check `idea.owner_id == user.id and idea.co_owner_id is None` → 400
  - Batch-load users pattern for list endpoint: collect IDs from owner + co_owner + collaborators, single query, build map
  - `.ralph/` directory is gitignored — must use `git add -f` to stage `.ralph/` files
  - Transient DB connection errors can occur between test classes (test DB dropped mid-suite) — re-running resolves
---

## US-003: User Directory Search API
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/authentication/views.py, services/gateway/apps/authentication/user_urls.py, services/gateway/gateway/urls.py, services/gateway/apps/authentication/tests/test_user_search.py, .ralph/test-manifest.json
- Tests: 606 Python tests passed (11 new), 361 Node tests passed. 1 pre-existing node error (PDFPreview).
- Learnings for future iterations:
  - User search endpoint lives in authentication app (users owned by auth), mounted at `api/users/search` via separate `user_urls.py`
  - Uses `Q(display_name__icontains=...) | Q(email__icontains=...)` for case-insensitive search across both fields
  - Excludes current user with `.exclude(id=user.id)`, limits to 20 results with `[:20]`, orders by `display_name` ASC
  - Auth uses `MiddlewareAuthentication` class from `apps.ideas.authentication` — same pattern as collaboration views
  - Ruff enforces import sorting — `django.db.models` import must be grouped with other django imports, not separate
---

## US-004: Read-Only Link Sharing API
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/ideas/views.py, services/gateway/apps/ideas/urls.py, services/gateway/middleware/share_link.py, services/gateway/gateway/settings/base.py, services/gateway/apps/ideas/tests/test_share_link.py, .ralph/test-manifest.json
- Tests: 614 Python tests passed (8 new), 361 Node tests passed. 1 pre-existing node error (PDFPreview).
- Learnings for future iterations:
  - share_link_token field already existed on Idea model (both core and gateway) — no migration needed
  - Share link endpoint uses `secrets.token_hex(32)` for 64-char hex token, placed in ideas views (not collaboration)
  - ShareLinkMiddleware placed after AuthenticationMiddleware in MIDDLEWARE list — needs authenticated user to add role
  - Middleware only validates tokens on `/api/ideas/` paths to avoid interfering with other endpoints
  - Middleware adds `share_link_viewer` to user.roles list for downstream role-based access control
  - Transient DB connection error between test classes persists — re-run resolves (known issue from US-002)
  - URL route: `<idea_id>/share-link` added to ideas URLs alongside collaboration routes
---
