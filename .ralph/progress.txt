# Ralph Progress Log — M16 (polish)
Started: 2026-03-11T17:44:30+0100

---

## Implementation Log

<!-- Ralph appends per-story entries below as it works. -->
<!-- Each entry should include: story ID, what was done, files changed, -->
<!-- decisions made, and any deviations from the spec. -->

---

## Codebase Patterns

<!-- Ralph records reusable patterns discovered during implementation. -->
<!-- These are extracted by the PRD Writer for future milestones. -->

---

## Deviations & Decisions

<!-- Ralph records any spec deviations or implementation decisions -->
<!-- that the Spec Reconciler should review. -->

## US-001: Universal Error Pattern — Enhanced Error Toast
- Status: PASS
- Attempts: 1
- Changes: frontend/src/components/common/ErrorToast.tsx (enhanced with buttons + retry counter), frontend/src/hooks/useErrorHandler.ts (new), frontend/src/__tests__/error-toast.test.tsx (new)
- Tests: 8 passed (T-14.1.01, T-14.1.03, T-14.1.04 all covered). Typecheck clean.
- Learnings for future iterations:
  - ErrorToast accepts onShowLogs/onRetry callbacks and retryCount/maxRetries props
  - useErrorHandler hook manages retry state: tracks count, enforces max, provides reset
  - Button component supports variant="outline" and variant="primary" with size="sm"
  - Toast component uses role="alert" and supports data-testid
  - Pre-existing Docker build failure (missing nginx.conf) and information-gaps-toggle test failure — not related to M16
---

## US-002: Error Log Modal — Technical Details
- Status: PASS
- Attempts: 1
- Changes: frontend/src/components/common/ErrorLogModal.tsx (enhanced with 4 sections), frontend/src/utils/errorLogger.ts (new), frontend/src/__tests__/error-log-modal.test.tsx (new), .ralph/test-manifest.json (added T-14.1.01-04)
- Tests: 8 passed (T-14.1.02 covered + errorLogger utility tests). Typecheck clean. No regressions in error-toast tests.
- Learnings for future iterations:
  - ErrorLogModal now accepts consoleLogs, networkResponse, and technicalDetails props alongside legacy errors array
  - errorLogger.ts provides console.error monkey-patch capture (max 20 entries), buildTechnicalDetails, buildNetworkResponse, buildErrorLogData helpers
  - utils directory created at frontend/src/utils/ for shared utilities
  - Support contact: support@commerzreal.de (hardcoded as SUPPORT_EMAIL constant)
  - Dialog component from shadcn/ui uses Radix primitives — data-testid passes through via spread props
---

## US-003: Idle Detection — Mouse Inactivity & Tab Visibility
- Status: PASS
- Attempts: 1
- Changes: frontend/src/hooks/useIdleDetection.ts (new), frontend/src/__tests__/idle-detection.test.tsx (new), .ralph/test-manifest.json (added T-15.1.01, T-15.1.02)
- Tests: 7 passed (T-15.1.01, T-15.1.02 covered + mouse reset, active on return, default timeout, tab focus recovery). Typecheck clean. No regressions.
- Learnings for future iterations:
  - useIdleDetection hook takes ideaId and optional idleTimeout (default 300s), uses useWsSend to send presence_update messages
  - Mouse movement throttled at 500ms to avoid flooding
  - Tab visibility uses document.visibilityState + visibilitychange event — sets idle immediately on hidden
  - Presence slice already supports "idle" state, PresenceIndicators already renders opacity-50 + grayscale for idle users
  - WebSocket consumer handle_presence_update already maps state='active' to 'online' for broadcast
  - No backend changes needed — the consumer already handles presence_update messages with idle state
  - Pre-existing test failure in information-gaps-toggle.test.tsx unrelated to M16
---

## US-004: Connection Disconnect on Prolonged Idle
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/websocket/consumers.py (added idle disconnect timer with asyncio), frontend/src/store/websocket-slice.ts (added isIdleDisconnected state), frontend/src/hooks/use-websocket.ts (detect close code 4008), frontend/src/components/common/OfflineBanner.tsx (idle disconnect message variant), frontend/src/__tests__/idle-disconnect.test.tsx (new, 5 tests), frontend/src/__tests__/use-websocket.test.ts (updated MockWebSocket for CloseEvent), 12 test files updated for isIdleDisconnected field, .ralph/test-manifest.json (added T-15.2.01)
- Tests: 5 new tests passed (T-15.2.01 covered + idle disconnect banner, no reconnect button, clears on online, store state). 447 total passed, 1 pre-existing failure. Typecheck clean.
- Learnings for future iterations:
  - Server uses close code 4008 for idle disconnect — frontend detects this to show idle-specific banner
  - Server idle timer uses asyncio.ensure_future + asyncio.sleep — cancelled on active presence_update or disconnect
  - _get_idle_disconnect_seconds() reads admin_parameters.idle_disconnect (default 120)
  - Adding new fields to websocket-slice requires updating preloadedState in ~14 test files that mock the websocket store
  - MockWebSocket.simulateClose() must pass a CloseEvent-like object with `code` property since use-websocket reads event.code
  - Chat/board inputs already locked when offline (selectIsOnline = false) — no additional locking needed
  - When idle disconnected, auto-reconnect is suppressed (reconnect on mouse move is US-005's responsibility)
  - Pre-existing test failures: idea-workspace.test.tsx (fetchIdea called with extra undefined), information-gaps-toggle.test.tsx
---

## US-005: Return from Idle — Reconnection & State Sync
- Status: PASS
- Attempts: 1
- Changes: frontend/src/hooks/useIdeaSync.ts (new), frontend/src/pages/IdeaWorkspace/index.tsx (integrated useIdeaSync), frontend/src/__tests__/idle-recovery.test.tsx (new, 5 tests), frontend/src/__tests__/idea-workspace.test.tsx (added QueryClientProvider), .ralph/test-manifest.json (added T-15.3.01)
- Tests: 5 new tests passed (T-15.3.01 covered + reconnect triggering, state sync, error handling). 452 total passed, 1 pre-existing failure (information-gaps-toggle). Typecheck clean.
- Learnings for future iterations:
  - useIdeaSync hook monitors isIdleDisconnected from store and listens for mouse movement to trigger reconnectNow()
  - On mouse move while idle-disconnected: dispatches setIdleDisconnected(false), calls reconnectNow()
  - After reconnect (connectionState goes 'online' after wasIdleDisconnected): refetches idea via REST and calls queryClient.invalidateQueries()
  - wasIdleDisconnectedRef tracks the idle disconnect state across renders to detect reconnect-after-idle vs normal reconnect
  - Adding useQueryClient() to IdeaWorkspaceContent required idea-workspace.test.tsx to wrap with QueryClientProvider (other test files already had it)
  - Banner disappearing, chat/board unlocking, and presence returning to online all happen automatically via existing store selectors when connectionState goes 'online' (setConnectionState also clears isIdleDisconnected)
  - Pre-existing test failures: idea-workspace fetchIdea extra undefined arg, information-gaps-toggle.test.tsx
---

## US-006: i18n Completion — Full Coverage
- Status: PASS
- Attempts: 1
- Changes: frontend/src/i18n/locales/{de,en}.json (292+ keys each), 25+ component files updated to use t(), frontend/src/__tests__/i18n.test.tsx (new), frontend/src/test-setup.ts (i18n init), services/notification/email/renderer.py (bilingual email), .ralph/test-manifest.json, tasks/prd-m16.json
- Tests: 460 passed, 1 failed (pre-existing URL.createObjectURL in information-gaps-toggle), 1 error (pre-existing). i18n tests T-16.1.01, T-16.2.01, T-16.2.02 all pass. Typecheck clean. Ruff clean.
- Learnings for future iterations:
  - Tests MUST be run from frontend/ directory (cd frontend && npx vitest run), not project root
  - ES import hoisting means localStorage.setItem runs AFTER import statements — use explicit i18n.changeLanguage() in test-setup.ts
  - Class components (ErrorBoundary) can't use useTranslation hook — import i18n directly and use i18n.t()
  - Non-component files (hooks like useRateLimit) also need direct i18n import
  - idea-workspace.test.tsx failure is pre-existing (module resolution), not caused by i18n changes
  - When subagents generate i18n key names, verify them against the actual JSON — mismatches are common
  - Email renderer uses Python with _EMAIL_STRINGS dict pattern for bilingual support
  - Language switcher already existed in UserDropdown.tsx via i18n.changeLanguage + localStorage
---
