# Ralph Progress Log — M9 (brd-pdf)
Started: 2026-03-11T03:48:23+0100

---

## Implementation Log

<!-- Ralph appends per-story entries below as it works. -->
<!-- Each entry should include: story ID, what was done, files changed, -->
<!-- decisions made, and any deviations from the spec. -->

---

## Codebase Patterns

<!-- Ralph records reusable patterns discovered during implementation. -->
<!-- These are extracted by the PRD Writer for future milestones. -->

---

## Deviations & Decisions

<!-- Ralph records any spec deviations or implementation decisions -->
<!-- that the Spec Reconciler should review. -->

## US-001: Summarizing AI Agent — 3 Generation Modes
- Status: PASS
- Attempts: 1
- Changes: services/ai/agents/summarizing_ai/agent.py, services/ai/agents/summarizing_ai/prompt.py, services/ai/fixtures/summarizing_ai_response.json, services/ai/tests/test_summarizing_ai.py, .ralph/test-manifest.json
- Tests: 36 passed, 0 failed (all AI agent tests pass, full suite 417 passed, frontend 265 passed)
- Learnings for future iterations:
  - SummarizingAIAgent follows exact ContextAgent pattern: BaseAgent → _execute() with SK direct chat completion, _load_mock_response() with fixture
  - System prompt uses Python f-strings (not Handlebars {{}} from spec) — consistent with all other agents
  - _apply_mode_logic() is a pure function that handles selective_regeneration (nullify locked sections) and section_regeneration (nullify all except target)
  - Mock mode _load_mock_response() overridden to apply mode logic on fixture data (so selective/section modes work in mock too)
  - Docker test container needs --build flag when source files change (cached COPY layer)
  - AI tests need DJANGO_SETTINGS_MODULE=ai_service.settings.test (not gateway.settings.test)
  - _make_input_data helper: use `if x is None` instead of `x or default` because empty lists are falsy
  - The PDF service Docker build fails pre-existing (missing apt packages) — unrelated to AI changes
  - SECTION_KEYS and SECTION_LABELS exported from prompt.py for reuse in agent.py and tests
  - _parse_response handles markdown code blocks from LLM output (strips ```json fences)
  - Fallback on JSON parse failure returns "Not enough information." for all sections with all-insufficient readiness
---

## US-002: BRD Generation Pipeline — Context Assembly & Fabrication Validation
- Status: PASS
- Attempts: 1
- Changes: services/ai/processing/brd_pipeline.py (new), services/ai/processing/fabrication_validator.py, services/ai/grpc_clients/core_client.py, services/ai/tests/test_brd_pipeline.py (new), .ralph/test-manifest.json
- Tests: 444 passed, 0 failed (27 new BRD pipeline tests, full suite 444 passed, frontend 265 passed)
- Learnings for future iterations:
  - BrdGenerationPipeline follows ChatProcessingPipeline pattern: version tracking, abort flags, step-by-step orchestration, lazy agent imports
  - FabricationValidator is purely heuristic (no AI): keyword extraction + fuzzy matching via difflib.SequenceMatcher
  - build_source_material() combines chat_summary, recent_messages content, and board node titles/bodies into a single string for validation
  - Fabrication flags are warnings (not blockers) — content is still returned, user decides
  - CoreClient.get_brd_draft() stub returns section_locks (dict) and allow_information_gaps (bool)
  - Context assembly extracts locked_sections by filtering section_locks dict for True values
  - Pipeline publishes two event types: ai.brd.generated (main event with all data) and ai.security.fabrication_flag (per-section monitoring events)
  - Error handling: SK/agent failures caught at pipeline level, returned as error status (not raised)
  - Pipeline test file targeting doesn't work in Docker (Django settings module import issue) — must run full pytest suite
  - _MATCH_THRESHOLD=0.75 for fuzzy matching, _FLAG_RATIO_THRESHOLD=0.5 for flagging sections
---

## US-003: BRD Draft REST API — GET Draft, PATCH Sections/Locks/Gaps Toggle
- Status: PASS
- Attempts: 1
- Changes: services/gateway/apps/brd/models.py (new mirror model), services/gateway/apps/brd/serializers.py, services/gateway/apps/brd/views.py, services/gateway/apps/brd/urls.py, services/gateway/apps/brd/migrations/0001_create_brd_drafts.py (new), services/gateway/apps/brd/tests/test_views.py (new, 17 tests), services/gateway/apps/ideas/urls.py, .ralph/test-manifest.json
- Tests: 461 passed, 0 failed (17 new BRD API tests, full suite 461 passed, frontend 265 passed)
- Learnings for future iterations:
  - Mirror model pattern for BrdDraft: managed=False, db_table='brd_drafts' — same as AdminParameter and ChatContextSummary
  - Unmanaged mirror models need a RunSQL migration with CREATE TABLE IF NOT EXISTS for test DB creation (Django test runner skips unmanaged models)
  - Auto-lock on edit: view update logic checks which section fields were modified and adds corresponding lock keys to section_locks dict
  - SECTION_LOCK_KEYS maps field names to lock keys by stripping 'section_' prefix (e.g. section_title → title)
  - BRD URL routing: nested under ideas URLs as path("<str:idea_id>/brd", include("apps.brd.urls"))
  - GET creates empty draft via get_or_create if none exists (all sections NULL, locks={}, gaps=false)
  - readiness_evaluation is read-only in PATCH — only AI pipeline updates it
  - Access control: owner, co-owner, or collaborator can access BRD draft (same pattern as ideas views)
  - Docker test container must be rebuilt (--build) when adding new migrations
---

## US-004: PDF Generation Service — WeasyPrint gRPC Service
- Status: PASS
- Attempts: 1
- Changes: services/pdf/generator/builder.py, services/pdf/generator/renderer.py, services/pdf/grpc_server/servicers/pdf_servicer.py, services/pdf/main.py, services/pdf/Dockerfile, services/pdf/requirements.txt, services/pdf/templates/brd_styles.css, services/pdf/tests/test_pdf_service.py (new, 20 tests), infra/docker/Dockerfile.test-python, requirements-dev.txt, pyproject.toml, .ralph/test-manifest.json
- Tests: 481 passed, 0 failed (20 new PDF tests, full suite 481 passed, frontend 265 passed)
- Learnings for future iterations:
  - pydyf 0.12.x broke WeasyPrint 62.x (removed `transform` method from Stream class). Must pin `pydyf>=0.10,<0.12` alongside weasyprint 62.x
  - PDF service Dockerfile needs `COPY . ./services/pdf/` with `PYTHONPATH=/app` to maintain `services.pdf` import structure
  - libgdk-pixbuf package is named `libgdk-pixbuf-2.0-0` (with hyphens) not `libgdk-pixbuf2.0-0` in python:3.12-slim
  - WeasyPrint needs libcairo2 system package in addition to libpango and libgdk-pixbuf
  - PDF tests must NOT have `__init__.py` in tests directory — causes namespace collision with other `tests/` packages when `services/ai` is on PYTHONPATH
  - Do NOT add `services/pdf` to PYTHONPATH (same namespace collision issue). Use absolute imports (`from services.pdf.generator...`) in tests
  - HTML builder uses `html.escape()` for XSS protection in user-provided BRD content
  - /TODO marker validation is a simple string search in section content (case-sensitive)
  - CSS uses `@page` rules for A4 portrait with page numbers and footer branding
  - WeasyPrint `HTML(string=html).write_pdf()` returns bytes directly, no BytesIO needed
  - Frontend Docker build fails pre-existing (missing nginx.conf) — not related to PDF changes
---
